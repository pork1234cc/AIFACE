# 小红书 AI 头像接单工具 MVP 方案（单内置风格版）

## 1. 项目目标

构建一个**面向商家内部使用**的 AI 头像接单工作台。
首版只内置 **1 款已验证风格**，将“小红书接单 → 用户发素材 → 生成头像 → 修改 → 交付”沉淀成固定流程，提高接单量与交付效率。

**核心目标：**

* 降低单笔订单的人工操作时间
* 降低提示词重复编写成本
* 降低返工时的混乱程度
* 为后续扩展多风格、多商家、多账号打基础

**一句话定义：**
这不是一个“通用 AI 绘图站”，而是一个 **头像接单生产工作台**。

---

## 2. MVP 核心定位

### 2.1 首版定位

只做一件事：

> 使用 **1 款内置风格**，根据客户上传的参考素材与本人照片，生成可交付的头像图。

### 2.2 首版建议内置风格

建议先做：

**风格名称：软糯蜡笔 Q 版半身头像**

**风格特征：**

* 大头小肩膀
* 半身头像
* 软糯可爱
* 轮廓略有颗粒感
* 五官简化
* 腮红明显
* 白底或浅色纯底
* 适合做微信头像 / 小红书头像 / 社交头像

### 2.3 首版不做

* 不做用户自助公开平台
* 不做多风格商城
* 不做会员体系
* 不做复杂局部编辑器
* 不做训练专属模型
* 不做自动接入小红书消息
* 不做多人合照风格化
* 不做无限次自动返工

---

## 3. 适用业务场景

### 3.1 当前业务链路

当前商家业务通常是：

1. 小红书接单
2. 用户发参考图
3. 用户发本人照片 1~4 张
4. 商家手动组织提示词
5. AI 出图
6. 商家挑图 / 修改
7. 发给客户确认
8. 返工或交付

### 3.2 现有痛点

* 每单都要重复整理素材
* 商家脑子里要记“哪张图负责什么”
* 同类订单重复写提示词
* 修改版容易乱
* 图片命名混乱，容易发错
* 生成成本和返工成本不可追踪

### 3.3 MVP 解决的问题

* 让订单结构化
* 让风格标准化
* 让生成流程固定化
* 让返工过程可追踪
* 让未来扩展多风格时不推倒重来

---

## 4. MVP 边界

### 4.1 输入

每笔订单允许上传：

* 1 张参考图（可选，用于参考元素）
* 1~4 张本人照片
* 订单备注
* 风格选择（首版固定只有 1 款）
* 元素选择项

### 4.2 输出

每笔订单输出：

* 1~4 张初稿候选图（可配置）
* 若干修改版本
* 最终交付图 1 张或多张

### 4.3 元素控制范围

首版建议只支持以下控制项：

* 脸型 / 五官：默认来自本人主照片
* 发型：主照片或辅助照片
* 眼镜：保留 / 去掉
* 衣服：跟本人 / 跟参考图 / 简化处理
* 背景：默认白底
* 画幅：默认 1:1

---

## 5. 产品形态建议

## 5.1 首版形态

**商家内部网页工作台**

原因：

* 适合上传多图
* 适合并排查看候选图
* 适合管理多个订单
* 适合后续扩展账号体系
* 开发成本可控
* 你后续也可先自己用，不急着给外部用户开放

## 5.2 为什么不是小程序

小程序不是不能做，而是不适合作为第一步：

* 图片管理和批量查看不如网页方便
* 后台工作流操作复杂
* 首版重点是“商家生产效率”，不是“消费者体验”
* 容易把重心带偏成一个前台产品

**结论：**

* 首版：网页工作台
* 后续：如有必要，再加小程序做客户提交入口或订单查询入口

---

## 6. 系统核心设计思想

## 6.1 核心原则

### 原则 1：风格固定，订单变量化

首版只内置 1 种风格，不让每一单都重新定义风格。

### 原则 2：人物信息与风格信息分离

* 风格库决定“怎么画”
* 用户照片决定“画谁”
* 订单参数决定“保留什么、改什么”

### 原则 3：先做“半自动生产”，不是“全自动交付”

生成后仍保留人工验收，确保可控。

### 原则 4：结构化优先，魔法提示词其次

程序保存的是：

* 素材角色
* 风格配置
* 订单参数
* 生成记录
* 返工记录

而不是只保存一坨提示词。

---

## 7. MVP 功能模块

## 7.1 模块列表

1. 订单管理
2. 素材上传
3. 风格配置读取
4. 生成任务执行
5. 候选图查看
6. 修改重生成
7. 最终交付
8. 成本与日志记录

---

## 8. 页面设计

## 8.1 页面一：订单列表页

字段建议：

* 订单编号
* 客户昵称 / 备注名
* 创建时间
* 当前状态
* 风格名称
* 当前版本数
* 最终是否交付
* 操作入口

**状态建议：**

* 待整理
* 待生成
* 已生成待选择
* 待修改
* 已完成
* 已关闭

---

## 8.2 页面二：订单详情页

分为 5 个区域：

### A. 基本信息区

* 订单编号
* 客户备注
* 风格名称
* 下单时间
* 当前状态

### B. 素材区

* 参考图
* 本人主照片
* 本人辅助照片 1~3
* 查看大图

### C. 生成参数区

* 发型来源
* 眼镜是否保留
* 衣服来源
* 背景类型
* 备注要求
* 候选图数量

### D. 生成结果区

* 候选图列表
* 每张图可标记：

  * 入选
  * 作废
  * 继续修改
  * 设为最终图

### E. 修改记录区

* 修改时间
* 修改指令
* 基于哪个版本修改
* 修改结果图

---

## 9. 风格库设计（可扩展核心）

首版虽然只做 1 款风格，但结构必须按“风格库”来设计。

## 9.1 风格库对象定义

每个风格是一个独立配置包。

### 风格对象字段建议

```json
{
  "style_id": "q_crayon_001",
  "style_name": "软糯蜡笔Q版半身头像",
  "status": "active",
  "cover_image": "style/q_crayon_001/cover.jpg",
  "reference_images": [
    "style/q_crayon_001/ref_01.jpg",
    "style/q_crayon_001/ref_02.jpg"
  ],
  "prompt_template": {
    "system_style": "...",
    "subject_rule": "...",
    "render_rule": "...",
    "negative_rule": "..."
  },
  "default_params": {
    "aspect_ratio": "1:1",
    "background": "white",
    "output_count": 4
  },
  "allowed_controls": {
    "hair_source": true,
    "glasses_keep": true,
    "clothes_source": true,
    "background_switch": false
  },
  "qa_checklist": [
    "是否保持该风格的大头Q版比例",
    "是否保留明显腮红",
    "是否有颗粒感轮廓",
    "是否保留客户主要发型特征"
  ],
  "version": "1.0.0"
}
```

---

## 9.2 首版风格配置建议

### 风格 ID

`q_crayon_001`

### 风格名称

软糯蜡笔 Q 版半身头像

### 固定风格规则

* 大头 Q 版比例
* 半身构图
* 白底
* 柔和配色
* 轮廓有轻微颗粒感
* 眼睛简化
* 鼻子极简
* 腮红明显
* 保留发型与主要配饰特征
* 避免写实皮肤纹理
* 避免复杂背景

### 默认输出

* 初稿输出 4 张

---

## 10. 数据结构设计

## 10.1 订单表 `orders`

```json
{
  "id": "order_20260920_0001",
  "customer_name": "张三",
  "source_channel": "xiaohongshu",
  "style_id": "q_crayon_001",
  "status": "draft",
  "note": "想要可爱一点，保留眼镜",
  "created_at": "2026-09-20T10:00:00",
  "updated_at": "2026-09-20T10:05:00"
}
```

## 10.2 订单素材表 `order_images`

```json
{
  "id": "img_001",
  "order_id": "order_20260920_0001",
  "image_type": "person_main",
  "file_path": "/uploads/order_xxx/main.jpg",
  "sort_index": 1
}
```

`image_type` 建议值：

* `reference`
* `person_main`
* `person_aux`
* `output_candidate`
* `output_revision`
* `output_final`

## 10.3 订单参数表 `order_params`

```json
{
  "order_id": "order_20260920_0001",
  "hair_source": "person_aux_1",
  "glasses_keep": true,
  "clothes_source": "person_main",
  "background": "white",
  "extra_requirement": "头发蓬松一点，脸不要太尖"
}
```

## 10.4 生成任务表 `generation_tasks`

```json
{
  "id": "task_001",
  "order_id": "order_20260920_0001",
  "task_type": "initial_generate",
  "base_image_id": null,
  "status": "success",
  "provider": "xxx_model_vendor",
  "input_count": 3,
  "output_count": 4,
  "prompt_snapshot": "...",
  "cost_estimate": 0.86,
  "created_at": "2026-09-20T10:10:00"
}
```

## 10.5 修改记录表 `revision_tasks`

```json
{
  "id": "rev_001",
  "order_id": "order_20260920_0001",
  "from_output_id": "out_002",
  "revision_instruction": "保留整体风格，去掉眼镜，头发更柔和",
  "status": "success"
}
```

---

## 11. 生成流程设计

## 11.1 首次生成流程

```text
创建订单
→ 上传素材
→ 选择主照片
→ 填写控制项
→ 读取风格配置
→ 自动组装生成请求
→ 调用图像模型
→ 返回候选图
→ 人工选图
```

## 11.2 修改流程

```text
选择一个候选图
→ 填写修改要求
→ 基于该候选图继续编辑
→ 返回修改图
→ 人工确认
→ 设为最终图
```

---

## 12. 提示词组装策略

## 12.1 不直接把风格写死在页面里

应由后端按结构自动组装。

### 结构建议

提示词由 4 部分组成：

1. **风格固定规则**
2. **人物来源规则**
3. **订单控制项**
4. **约束与负面要求**

---

## 12.2 伪模板示意

```text
请根据输入图片生成一张单人头像插画。

【风格规则】
采用“软糯蜡笔Q版半身头像”风格：
- 大头小肩膀
- 半身构图
- 白色纯背景
- 柔和配色
- 轮廓有颗粒感
- 五官简化
- 腮红明显
- 整体可爱、软糯

【人物来源规则】
- 本人主照片用于确定脸型与主要五官特征
- 辅助照片用于补充发型与配饰
- 若有参考图，仅参考局部元素，不复制原人物身份

【订单参数】
- 发型参考：辅助照片1
- 眼镜：保留
- 衣服：跟随主照片
- 背景：白底

【输出要求】
- 1:1 头像
- 输出4张候选图
- 保持风格统一
- 避免写实皮肤质感
- 避免复杂背景
```

---

## 13. 可扩展性设计

这是本方案最重要的部分。
虽然 MVP 只做 1 款风格，但架构必须为未来扩展留口。

## 13.1 扩展方向一：多风格

后续只需新增：

* 风格配置
* 风格参考图
* 展示封面
* QA 验收规则

无需改动订单主流程。

### 后续可扩展风格示例

* 日系清透二次元
* 纸雕 Q 版
* 怀旧复古插画
* 潦草涂鸦风
* 3D 公仔风
* 水墨人物风

---

## 13.2 扩展方向二：多商家账号

增加：

* `users`
* `teams`
* `merchant_id`
* 数据隔离
* 角色权限

可支持多个商家同时使用一个系统。

---

## 13.3 扩展方向三：前台用户提交入口

未来可以加：

* 客户提交照片页
* 风格选择页
* 支付页
* 订单查询页

但这些都属于第二阶段，不影响当前后台工作台设计。

---

## 13.4 扩展方向四：自动报价与套餐

根据风格、输出数量、是否含修改次数，自动给出套餐：

* 标准款
* 高级款
* 多张图款
* 情侣款

---

## 13.5 扩展方向五：风格效果评估

未来可增加：

* 首次通过率
* 单风格平均返工次数
* 单风格平均成本
* 单风格交付成功率

帮助筛选真正适合商用的风格。

---

## 14. 技术方案建议

## 14.1 推荐技术栈

考虑你当前习惯，建议：

### 前端

* Next.js
* TypeScript
* Tailwind CSS

### 后端

* FastAPI
* SQLAlchemy
* Alembic
* SQLite（MVP）/ PostgreSQL（后续）

### 文件存储

* 本地存储（MVP）
* 对象存储（后续）

### 任务队列

MVP 可先简单处理：

* 先同步请求
* 或数据库轮询任务

后续再升级：

* Celery / RQ / 自定义 Worker

---

## 14.2 模块分层

```text
前端工作台
↓
后端 API
↓
订单服务 / 风格服务 / 生成服务 / 文件服务
↓
数据库 + 文件存储 + 图像模型API
```

---

## 15. API 设计建议

## 15.1 订单相关

* `POST /api/orders` 创建订单
* `GET /api/orders` 获取订单列表
* `GET /api/orders/{id}` 获取订单详情
* `PATCH /api/orders/{id}` 更新订单信息

## 15.2 素材相关

* `POST /api/orders/{id}/images` 上传图片
* `DELETE /api/images/{id}` 删除图片

## 15.3 生成相关

* `POST /api/orders/{id}/generate` 初次生成
* `POST /api/orders/{id}/revise` 基于候选图修改
* `GET /api/tasks/{id}` 查询任务状态

## 15.4 风格相关

* `GET /api/styles` 获取风格列表
* `GET /api/styles/{id}` 获取风格详情

---

## 16. MVP 开发优先级

## 16.1 P0（必须做）

1. 风格配置结构
2. 创建订单
3. 上传图片
4. 设置主照片 / 辅助照片
5. 设置基础控制项
6. 调用模型生成候选图
7. 查看候选图
8. 基于候选图修改
9. 设置最终图
10. 基本日志记录

## 16.2 P1（次优先）

1. 成本统计
2. 订单搜索筛选
3. 图片批量预览
4. 删除无效版本
5. 简单的风格展示页

## 16.3 P2（后续）

1. 多商家账号
2. 客户前台入口
3. 支付与套餐
4. 自动报价
5. 数据报表

---

## 17. 验收标准

## 17.1 MVP 是否算跑通

以下条件满足即可认为 MVP 成功：

### 功能层

* 能创建订单
* 能上传参考图和本人照片
* 能按内置风格生成候选图
* 能对候选图做修改
* 能选定最终交付图

### 业务层

* 能连续处理真实订单
* 商家不需要每次重写提示词
* 单笔订单流程明显更顺
* 图片与版本不混乱

### 数据层

* 能记录每笔订单的：

  * 素材
  * 参数
  * 生成记录
  * 修改记录
  * 最终结果

---

## 18. 成本与效率观察指标

MVP 上线后建议记录以下数据：

### 订单维度

* 每单生成次数
* 每单修改次数
* 每单最终交付耗时
* 每单成功交付率

### 风格维度

* 首次通过率
* 平均返工次数
* 平均模型成本
* 商家主观满意度

### 经营维度

* 每日处理单数
* 人均处理单数
* 单笔订单毛利空间

---

## 19. 风险点

## 19.1 风格稳定性风险

同一风格多次生成结果可能有差异。
解决：保留人工验收，不承诺全自动交付。

## 19.2 人物不像风险

Q 版头像五官高度简化，可能导致“像不像”争议。
解决：在风格文案里提前明确这是风格化头像，重点保留发型、脸型、配饰等识别元素。

## 19.3 返工失控风险

客户一句“再改改”可能无限蔓延。
解决：流程上限制修改轮次；系统层保留修改记录。

## 19.4 数据混乱风险

图片多、版本多、订单多后容易出错。
解决：从首版就采用结构化订单管理，不走纯文件夹手工管理。

---

## 20. 实施里程碑建议

## 阶段 1：最小可运行版本

目标：自己能真实用起来

内容：

* 1 款风格配置
* 订单管理
* 素材上传
* 初次生成
* 修改生成
* 最终图标记

周期建议：3~5 天做出第一版骨架

---

## 阶段 2：真实订单验证

目标：验证流程是否真的省时间

内容：

* 用 10~20 笔真实或模拟订单测试
* 记录返工次数和交付耗时
* 修正风格配置与提示策略

周期建议：3~7 天

---

## 阶段 3：沉淀可复制版本

目标：为扩展多风格做准备

内容：

* 抽象风格配置
* 优化提示模板
* 增加成本统计
* 增加简单筛选与搜索

---

## 21. 最终建议

对于你当前阶段，最优路径不是先做一个大而全的平台，而是：

> **先用 1 款内置风格，把商家接单生产流程跑顺。**

先解决：

* 订单怎么整理
* 图片怎么归类
* 风格怎么固定
* 修改怎么留痕
* 最终怎么交付

再解决：

* 多风格
* 多商家
* 客户自助
* 支付与分发

---

## 22. 一句话结论

**这个 MVP 的本质，不是“做一个 AI 画图产品”，而是“做一个可复制的头像接单生产流水线”。**

先把第一款风格做成稳定产品，再扩成风格库。
先把厨房跑顺，再决定菜单印多厚。

---

如果你要，我下一步可以继续直接给你补这 3 份配套文档，保持同一套结构：

1. `PRD.md`
2. `数据库表结构设计.md`
3. `前后端接口设计.md`
