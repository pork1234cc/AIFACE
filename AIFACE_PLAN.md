# AIFACE 会话交接与续做计划

## 最新增补：素材上传卡片（2026-09-21）

- 素材区仅一个上传入口，只显示已上传的素材图。自动编号，卡片底部直接显示“素材N”和“移出”；宽屏四列，图片与主图采用同一尺寸规则。
- 主图片已有图片时，“替换”和“移出”也在缩略图底部同一行，避免纵向堆叠。这是对主图卡片布局的补充规则。
- 移出后其他编号保持不变，新上传优先补最小空号，避免完整提示词中已写的素材编号指向另一张图。以下历史方案里的空素材位展示不再适用。

## 最新增补：生图格式与素材数量（2026-09-21）

- 订单生图质量参数固定为 `high`。风格和尺寸后新增 PNG/JPEG/WebP 格式选择，默认 PNG；提交时随任务快照传给供应商。
- 素材位置从前三项继续按顺序追加，不再限制为三张；0009 迁移移除活动输入数量触发器，主图仍唯一。以下历史阶段中的四图限制已被此规则覆盖。
- 交付区域不再显示“当前交付图”标题，图片及下载、继续修改操作保留。

## 最新增补：手动生成风格示意图（2026-09-21）

- 已实现自定义风格卡片名称同行右侧的“生成示意图／重新生成示意图”，普通字重；内置只读，保存提示词不自动生成。
- 统一虚构人物底图位于 styles/preview/base-portrait.png，说明和 imagegen 完整提示词见同目录 README.md。
- 0008_style_previews 已应用默认库；独立任务队列共用 Worker，幂等、版本快照、下载恢复和受理不明核对已接入。失败保留旧封面，版本改变标记待更新。
- 后端 123、前端 54 项测试及生产构建通过；隔离浏览器验证完整流程，没有真实供应商请求。详细证据见 [示意图验收](docs/style-previews-validation.md)。
- 已在确认无活动任务后通过启动脚本重启完整本地服务。注意该脚本监督三个子服务，单独停止 Worker 会触发整套退出。

## 最新增补：自定义风格（2026-09-21）

- 用户确认内置风格提示词不允许编辑，另提供自定义风格。已实现创建、查看、编辑、订单选择与生成快照；详情窗口 440px，隐藏英文编号和版本。
- 迁移 0007_custom_styles 已应用默认库，新增自定义风格表；默认库未写入测试风格。原后端热重载已恢复，健康检查通过。
- 后端 113、前端 52 项测试及生产构建通过；隔离浏览器完成创建/编辑/刷新/选择/模拟生成验收。无真实供应商调用。
- 设计和 API 见 docs/architecture.md、docs/api.md；详细证据见 [自定义风格验收](docs/custom-styles-validation.md)。

## 最新实施结果：统一创作已落地（2026-09-21）

- 已完成 A/B/C 及 D 的本地模拟验收：统一配置 schema_version=2、main/material 角色、结构化修改项、新风格 2.0.0、11 种比例、预览提交共用校验、不可变快照。
- 页面左侧为底图、风格、素材、修改项与输出设置，右侧直接生成/交付；独立风格页，提示词详情，版本对比，继续修改清空旧项，支持明确复用历史要求。
- 新默认存储 `storage/unified`，数据库 `storage/unified/database/aiface.sqlite3` 已迁移至 0006；新 ORM/Worker 仅访问 v2 表。旧默认库只读检查前后均为 2 个成功任务，没有未结束任务；未改旧库或图片。
- 完整检查通过：后端 104 项、前端 51 项，lint/格式/类型/依赖检查及生产构建。浏览器通过纯风格生成、素材映射修改、自动交付、版本快照与底图对比、新底图草稿刷新恢复，以及 1440/800/390px 布局。
- 浏览器模拟提交 2 次，真实 POST 0 次；真实人物一致性、多人及局部修改效果仍待实图验收。完整证据见 [统一流程验收](docs/unified-validation.md)。
- 原 3000/8000 服务未停止；使用新版前需正常退出旧进程，再运行 `start-aiface.cmd`。本次 13000/18000 隔离服务已关闭，测试数据/构建保留。

## 最新修改方案：以主照片为基础的统一创作流程（2026-09-21）

状态：本节方案已实施并通过本地模拟验收，真实模型效果仍待验收。下方阶段 5、UI 修改包等内容是现有实现记录，不代表本方案已经完成。后续优先完成本方案，再继续真实效果验收。用户最新确认：不兼容旧订单，按新业务结构直接设计和实现；本条覆盖下方历史记录中的旧订单兼容要求。

### 1. 核心原则与纠正记录 ✅

**每次创作都以主照片为基础；风格控制表现方式，素材提供指定元素，文字限定修改范围；未被要求修改的内容默认保留。**

- 不拆分“照片风格生成”和“参考图局部编辑”两种业务模式，不提供模式切换。它们是同一流程中不同的输入组合。
- 主照片是基础画面，不必是客户本人的照片，也可以是客户喜欢的双人版式图。主照片决定默认构图、人物和物品数量、位置、姿态、背景等内容。
- 仅上传主照片并选择风格，即可生成；无需补传素材或强制填写局部修改项。
- 有素材时，只取素材中明确指定的元素，替换主照片中的指定对象或部位，不顺带复制素材的背景、服装或其他人物特征。
- 裁剪、取景与比例调整通过输出设置及补充要求表达；不因存在裁剪而划分另一种创作模式。
- 风格化可以改变绘画表现，但不得默认增加或移除人物、替换背景、重排版式、改变动作。风格化的人体夸张与构图保留需要通过实际效果验收评估。
- 纠正记录：此前把两种输入情形拆成两个模式，并把主照片限定为身份来源，理解错误。后续界面、数据、提示词和测试必须围绕统一底图编辑设计，不能只改页面名称。

- 方案调整记录：用户明确旧订单不要兼容；取消旧字段映射、旧订单转换展示、旧任务运行／恢复适配及旧订单兼容验收，不再为历史业务结构增加兼容层。新流程自身的版本历史和任务恢复仍保留。

### 2. 现状差异与修改范围

根据现有设计文档和风格文件核对：

| 当前规则或配置 | 目标规则 |
| --- | --- |
| 主照片 person_main、辅助 person_aux、参考 reference 三种角色 | 界面统一为主照片与素材图，素材用途通过修改项表达 |
| 主照片主要确定脸型与五官 | 主照片确定整个基础画面，身份可按明确要求用素材替换 |
| 发型来源、眼镜保留、服装模式等固定控制项 | 可选修改项：对象、修改内容、素材来源及保留要求 |
| q_crayon_001 1.1.0 包含半身、白底、1:1、单人等硬约束 | 新版风格聚焦表现方式，构图和内容由主照片及明确要求决定 |
| 架构约定首版固定白底与 1:1 | 默认保留背景及原图取景意图，输出比例按支持能力处理 |
| 创作流程与主工作台入口内容重复 | 移除独立创作流程导航，保留主工作台与订单操作入口 |
| 已有素材/要求与生成/交付两栏基础 | 细化为左侧统一生成配置，右侧生成按钮及直接显示结果 |

本方案覆盖旧方案中冲突的角色、固定构图和固定背景规则。新流程保留每次一张、唯一当前交付、版本历史、任务恢复及终态只读等已确认约束；这些约束不表示兼容旧订单。上一轮分析中“生成张数可选”的建议不采用，仍固定每次一张。

### 3. 图片与修改项定义

#### 3.1 主照片

- 首次生成使用一张上传主照片，必须明确选定，不能从多张素材中自动猜测。
- 主照片可以是单人照、多人照、包含物品的图片或客户喜欢的版式。
- 页面标注“本次编辑底图”，说明未指定修改的内容默认保留。
- 继续修改沿用现有当前交付图作为本次底图；明确展示当前底图缩略图及版本。原始上传主照片仍保留，可查看对比。
- 原始主照片、本次底图、当前交付图是不同概念，不能因为名称统一而覆盖资产或历史关联。

#### 3.2 素材图

- 素材图为可选输入，提供脸部身份、发型、手势、服装、道具等指定元素。
- 同一素材可以被多个修改项引用；同一修改项可以引用多个必要素材，实际发送图片按 ID 去重。
- 单次模型输入沿用最多四张：一张底图加最多三张不同素材图。当前订单输入素材额度也沿用现有四张约束，本期不擅自扩为无限素材库。
- 未参与本次修改的素材不自动发送；风格卡片封面也不自动作为模型输入。
- 超额、跨订单、素材失效、缺失引用必须提示，不静默丢弃或补入图片。

#### 3.3 修改项

建议使用可重复添加的结构化修改项，具体字段名在实施时落实：

| 字段建议 | 含义 | 示例 |
| --- | --- | --- |
| target_description | 主照片中的目标对象或部位 | 画面左侧人物的脸部 |
| change_type | 修改用途，可有自定义类别 | 脸部身份、发型、手势 |
| source_asset_ids | 本项素材来源，可为空 | 客户脸部素材 A |
| instruction | 具体改成什么 | 替换为素材 A 中人物的身份特征 |
| preserve_instruction | 本项明确保留要求，可选 | 保留底图表情、朝向和身体姿态 |

“左侧”以画面视角表述；手部等容易歧义的目标应明确到人物和部位。没有素材也可填写文字修改项。纯风格转换允许修改项为空。第一版采用目标描述和素材绑定，不强制增加框选、分割或蒙版工具。

修改手势允许为自然衔接调整邻近手腕或必要的局部手臂，不因此放开全身动作及其他区域。必要调整的实际范围和质量由效果验收验证，不能宣称逐像素锁定。

### 4. 订单详情与导航方案

#### 4.1 导航

- 主工作台承载订单、任务和进入订单的入口。
- 取消左侧独立“创作流程”菜单及重复内容；旧锚点或入口做兼容引导，避免链接失效。
- 保留“风格”入口，点击后呈现内置风格卡片列表；具体路由沿用项目组织方式，不再指向重复工作台内容。

#### 4.2 左侧：统一订单生成配置

建议桌面约 40% 宽度，按以下顺序组织：

1. 订单基本信息，非核心信息可折叠。
2. 主照片／本次编辑底图，显示预览及来源版本。
3. 风格卡片，支持选定风格和“保持原图风格”。
4. 素材图及修改项，可选；明确展示每项素材到目标的对应关系。
5. 补充要求：裁剪、取景、全局保留要求及其他文字修改。
6. 输出设置：比例及供应商确实支持的尺寸选项，每次固定一张。
7. 可展开“本次生成提示词”，用于检查当前配置组装出的完整要求。

首次和继续修改复用同一套配置语义，避免首次表单与修改表单维护不同的图片定义。继续修改显示本轮修改项，不自动重施上一轮已完成的替换要求；如需复用历史项，由用户明确选择。

#### 4.3 右侧：生成与结果

建议桌面约 60% 宽度，顶部为生成按钮及任务状态，下方直接显示结果。

- 点击生成后就地显示进度或占位；成功后直接展示单张结果，不跳转其他页面。
- 成功沿用现有规则，自动设为当前交付图；失败保留上一张结果和本次配置。
- 提供放大、下载、与本次底图对比、继续修改、查看历史和切回旧版。
- 继续修改须让用户清楚当前以哪张图为底图，配置快照与对应结果绑定。
- 生成中及提交状态不明时，沿用现有并发和恢复约束；配置草稿的变化不改变已提交任务。
- 小屏改为上下排列，配置在上、生成和结果在下；避免横向溢出。
- 下载不自动完成订单；完成／关闭及只读行为保持现有规则。

### 5. 风格卡片与提示词重构

- 卡片包含示例图（没有真实封面时明确显示占位）、名称、说明、选中状态及底部“查看提示词”按钮。
- 点击卡片选择风格；按钮单独打开弹窗或侧栏，不顺带切换风格。
- 展示风格 ID、版本、风格基础提示词及实际相关设置；与“本次生成提示词”明确区分。
- “保持原图风格”是无需额外风格转换的选择，不伪装成已有内置风格包。
- 现有 q_crayon_001 新建版本：保留蜡笔笔触、柔和配色等表现特征，移除默认强制白底、单人、半身、1:1、禁止额外人物等可能破坏原图的规则；同步名称说明和验收标准。用户原图已有的人物不能因“避免额外人物”被删除。
- 不能把“输出一张图片”写成“只画一个人物”；单张图片可以包含左右两个人物或物品。
- 当前仅核实一份内置风格配置，卡片列表如实展示现有风格；本方案不虚构其他已内置风格。
- 新流程的每次提交记录风格版本及快照，后续风格调整不影响该流程中已生成结果的追溯；不加载或适配旧订单的风格快照。

提示词组装建议顺序：底图及默认保留规则 → 按实际输入顺序标记素材 → 逐条修改映射 → 风格表现 → 补充要求及取景 → 输出约束。

预览与实际提交必须共用组装和校验逻辑。默认保留规则可被明确修改要求覆盖；具体要求之间存在冲突时提示用户澄清，不用隐藏优先级猜测。不增加用于自动解析需求的额外付费模型调用。

比例和取景分开处理：“3:4”只定义画布，“裁掉手部、保留面部和发型”定义取景。先核查供应商支持比例，不能把任意原图比例当作已支持参数；原比例不受支持时展示实际可选值和取景影响，不静默拉伸或宣称精确保留。

### 6. 数据与 API 方案（不兼容旧订单）

此节是实施设计建议，不代表现有接口已经具备这些字段。

- 配置增加明确版本标识，并保存本次底图、风格选择、修改项、补充要求和输出设置。按统一创作模型设计参数与任务快照，不保留旧字段兼容分支，不建立无必要的通用规则框架。
- 前端可统一流程，后端可保留 initial／revision 区分新流程中的首次生成与继续修改，无需为了移除模式切换而抹平版本血缘。
- 现有 params、ready、prompt-preview、generate、revise 等相关接口同步支持统一配置；具体请求结构在实施时同步到 docs/api.md。
- 风格详情提供可展示的基础提示词和版本，列表提供卡片所需元信息；不混入密钥或供应商鉴权数据。
- 提交事务重新验证同订单归属、图片可用状态、输入数量、修改项引用及请求摘要，保存有序输入、完整修改关系、风格与最终提示词快照。
- 不兼容旧订单、旧素材角色、旧控制字段、旧请求结构及旧任务快照；不做字段映射、订单转换、双协议分支或旧订单专用展示。
- 新流程仅接收符合新契约的配置，旧格式不自动转换；新工作台及 Worker 不加载或领取旧订单任务。
- 数据结构直接服务新业务，允许按需要调整模型及建立新结构；部署时采用独立的新数据存储，避免为运行新流程依赖旧数据转换。具体初始化方式在实施时核实。
- 切换前只读检查是否存在未结束的旧任务，记录状态并避免新 Worker 误领；不开发旧任务继续执行、恢复或重放的兼容逻辑。有实际未结束任务时单独明确处置，不默认取消或重新提交。
- “不兼容旧订单”不等于授权删除旧数据库或图片。本次及常规实现不清空、删除或不可恢复覆盖旧数据；确需清理时另行说明具体范围并取得确认。
- 保留本地幂等、提交不明核对、仅重试下载、单订单一组活动任务、修改成功更新交付和失败保留原图。

### 7. 分阶段实施清单（本地实施完成） ✅

本轮已按以下子任务逐阶段完成；D 阶段真实模型效果单独保留为后续验收：

| 阶段 | 工作 | 主要涉及位置 | 完成依据 |
| --- | --- | --- | --- |
| A：新业务契约 | 落实统一配置结构、新数据存储与初始化、风格版本和比例能力 | docs/architecture.md、docs/data-model.md、docs/api.md、相关 schemas | 文档与新接口一致，不包含旧订单兼容层 |
| B：业务与提示词 | 图片用途、修改项校验、统一组装、预览提交一致、快照及新结构初始化 | backend/app/services、schemas、models、migrations、styles | 每模块先写复现／回归测试，lint 与受影响测试通过 |
| C：页面交互 | 导航去重、风格卡片、配置栏、素材映射、就地出图 | frontend/src/app、components/workspace、components/orders、types/lib | lint、类型检查及相关交互测试通过 |
| D：端到端验收 | 新流程场景、任务隔离、失败恢复、移动端和文档同步 | backend/tests、frontend/tests、docs/acceptance.md、验收记录 | 模拟流程通过，真实效果单独记录 |

实施前读取对应模块设计和 frontend/AGENTS.md。Python 使用项目 .venv 并核验解释器；不重建环境。逐模块验证，不等全部改完才检查。

真实模型效果验收前说明场景及预期生成 POST 次数，不为恢复上下文重复历史幂等探测；报告区分真实请求、远端任务和计费情况。模拟通过不等于真实人物一致性或局部编辑质量通过。

### 8. 验收场景

| 场景 | 预期 |
| --- | --- |
| 一张主照片加风格，无修改项 | 可直接生成一张；保持原人物、物品、动作、背景和布局，仅改变表现风格 |
| 双人底图只换风格 | 两个人物及左右关系保留，不因旧单人规则删人 |
| 带手部原图加半身裁剪说明 | 按取景要求裁掉手部，不重新设计姿势，不把人物手部理解为需删除的身体部位 |
| 双人底图加脸、发型、手势三张素材 | 只替换指定对象和指定元素；一张底图加三张素材满足四张上限 |
| 同素材用于两个修改项 | 模型输入去重，两个映射都准确保留 |
| 不选新风格，仅局部修改 | 保持底图风格和未修改区域，素材背景不迁移 |
| 空素材／空修改项 | 纯风格转换不报必填错误；缺失底图禁止提交 |
| 素材移出、跨单引用或输入超限 | 提交前及服务端明确阻止，不静默忽略 |
| 同一对象要求互相矛盾 | 对可识别冲突明确提示，不能承诺自动理解全部自然语言冲突 |
| 预览后配置变化 | 实际提交使用当前有效配置并保存新快照；旧结果快照不变 |
| 继续修改、失败、切回历史 | 底图版本明确，成功更新当前图，失败保留旧图，历史可追溯 |
| 新旧运行隔离 | 新流程使用新数据结构；旧格式不转换，旧订单不加载，旧任务不被新 Worker 领取；没有清理或覆盖旧数据 |
| 风格查看按钮 | 独立展示对应版本基础提示词，不误切风格，不冒充最终提示词 |
| 桌面与小屏订单页 | 桌面两栏、右侧直接出图；小屏上下排列，无溢出 |

### 9. 核实结果与真实效果待办

- 已依据 image2.md 开放 11 种标准比例及尺寸映射，并展示取景影响；本次未逐一远端实测。
- 新蜡笔风格对多人版式、身份保持、裁剪和局部替换的真实效果，尤其风格化夸张是否影响用户希望保留的内容。
- 新数据存储已初始化；迁移与 Worker 隔离测试通过，旧数据库保持原状。

以上属于实施核实项；统一流程、主照片定义和按指定素材局部替换的业务方向已经确认，不重新讨论或退回双模式。

---

## 最新补充：UI 修改包已应用并通过核心浏览器验收（2026-09-21）

- 用户授权按分析计划替换，已应用 `AIFACE_UI_Fix_20260921` 1.0.0：11 个替换、12 个新增，与基线 `92bdd3e` 匹配。应用后 23 个文件校验一致。
- 原始字节备份 `.aiface-ui-backups/20260921T034701Z-daa305a1`；无后端/依赖/默认业务库修改，无真实供应商调用。
- 前端 51 项测试、类型检查、ESLint、`.next-ui-fix-build` 生产构建通过；构建配置恢复后类型检查通过。
- 用户手动启动 13000 后完成 Chrome 核心验收：导航、390/800/1024/1440 布局、首次单图、修改附加上限、成功自动替换、切回旧版、完成后只读下载、空单关闭、草稿返回恢复及提交不明核对重试。页面 error 日志为空。
- 发现并修复跨页锚点高亮：只有 `workspace-shell.tsx` 有意偏离包内版本；原生锚点保证地址与订阅一致，普通路由仍用 Next Link。修复前浏览器断言失败、修复后通过；对应 ESLint、35 项测试、类型及最终生产构建通过。另有 24 项后端相关回归通过。
- `storage/qa-ui-fix-20260921/report.json` 保存结果；浏览器模拟 submit 4 次，3 张模拟图，真实 POST 0 次。验收订单均无未结束任务，不能声称包内全部 28 项清单均已跑完。
- 本次 13000/18000 隔离进程已停止，视口恢复，测试标签关闭，类型配置恢复且检查通过；测试数据/备份/构建保留。
- 应用前默认 3000/8000 在运行，停止前端后原启动脚本可能联动清理整组服务。自动审批拒绝了前端启动，仅返回 `blocked by policy`；已请用户清除隔离环境变量后用 `scripts/start-dev.ps1` 恢复默认服务，继续时先查端口。
- 完整记录见 [UI 修改包验收](docs/ui-fix-validation.md)。原包安装器因本地高亮修复会拒绝重装/回滚，不能强制覆盖。下方阶段 5 业务规则继续有效，阶段 6 真实效果/费用验证仍未完成。

## 最新状态：阶段 5 单图交付闭环完成（2026-09-21）

### 已确认业务规则 ✅

上传素材后按风格只生成 **1 张，直接作为当前交付图**。需要时基于当前图修改；修改成功自动使用最新图，失败保留原图，旧版本保留且可切回。输入最多 4 张、基础图占 1 张、附加最多 3 张，修改无硬上限。

本条覆盖下方历史“两张初稿、候选入选、多图 ZIP”要求。旧双图记录保留，系统不擅自选其中一张交付，由商家在版本历史指定。

### 本次完成 ✅

- 新生成只创建一个任务；风格版本 1.1.0、预览、API 和页面数量一致。
- Worker 成功保存图片时同事务更新唯一当前交付，保留撤销历史；可切回旧版，同当前版本重放不产生重复记录。
- 当前单张原图下载；下载不代表已交付客户，也不自动完成订单。完成/关闭独立确认，终态只读但允许下载。
- 0004_deliveries、0005_single_generation 迁移。默认库先在线备份到 `storage/database/before-stage5-20260921-005105.sqlite3` 再升级；原订单、资产、任务和幂等动作逐字段一致，外键检查与 Alembic check 通过。没有替旧业务订单选图。
- 页面主线改为当前图、修改及版本历史；旧审阅接口保留兼容。当前交付图禁止直接作废；未结束任务禁止版本切换/撤销、完成或关闭。

### 验证与收尾 ✅

- 后端 88 项、前端 10 项测试、Ruff/格式、ESLint、TypeScript、pip check 和独立目录生产构建通过。末轮发现版本切换 API 漏未结束任务约束，先复现 pending/running/needs_attention 三例，再补同事务检查，交付 8 项回归通过。
- Chrome 实际验收首次单图、修改自动替换、切回首版、下载不完成、完成后刷新只读且可下载、无结果不能完成但可关闭；390px 无横向溢出，图片正常，视口已恢复，测试标签已关闭。
- 本次 **真实供应商 POST 0 次，本地模拟 submit 2 次**；1 次首次生成、1 次修改，共 2 张 512×512 模拟图，不代表模型风格/人物/修改效果。
- `storage/qa-stage5-20260921/report.json` 保存订单、任务、版本及原图下载字节校验；详见 [阶段 5 验收](docs/stage5-validation.md)。manifest 的旧“两图成功/部分失败”标签沿用旧脚本，只是测试标识，本次首次目标确为 1。
- 12 份文档检查通过、65 个源码摘要归档，62 个静态产物/日志/报告密钥匹配命中 0；默认业务记录末轮复验仍与备份一致。
- 使用原 `.venv`，无依赖安装、无 `.env` 改动、无数据删除、无 Git 初始化/提交/部署。测试文件及 `.next-stage5-build`、`.next-stage5-qa` 保留。
- 隔离前端由用户手动启动；验收后仅停止本次 13000/18000 进程。默认 3000/8000 开工与收尾均未运行，未停止默认服务。无遗留模拟或真实待执行任务。Next.js 类型路径恢复默认 `.next`。

### 下一步 ⏳

阶段 6：整体试用、真人与风格/修改效果验收、供应商费用和幂等有效期核实。不要为恢复上下文重复历史 4 次真实探测；新增真实联调前说明目的及预期 POST 次数。submitted_attempt_count 只是进入提交阶段的次数，不等于真实 HTTP 到达量或账单。

### 修正记录

用户纠正后，必须同步任务数量、模型约束、风格版本、提示词、API、页面和验收，不能只把“初稿”改成“交付图”而继续生成两张。用户已确认自动使用最新修改图，不再等待替换方式答复。

接续口令：**继续 AIFACE，读取 AIFACE_PLAN.md，从阶段 6 开始。**

---

## 阶段 4 历史交接（以下不是最新状态）

更新日期：2026-09-21。本节覆盖下方历史交接。用户要求“补阶段 3 浏览器验收并开始阶段 4”，现已完成阶段 4 修改与人工选择的实现和模拟验收，下一阶段为阶段 5 交付闭环。

### 本次完成 ✅

- 阶段 3 浏览器：初稿两张、部分成功只补失败位置、关联已有远端任务、确认未受理后补生成、接受风险重发；风险勾选约束、历史未知记录和成功图保留均实际点击验收。
- 阶段 4 后端：`services/revisions.py`、修改/审阅/待修改标记/统计 API。基础图同单、成功、未作废且文件完整，明确附加最多 3 张当前素材；单任务、单位置、幂等及单单一组。继承基础图风格快照，旧控制项只作记录，不覆盖本次“去掉眼镜”等修改要求。
- 阶段 4 页面：候选入选、作废及恢复；修改表单、基础图预览、附加素材选择、单张修改结果、基础版本链接、轮次及尝试统计。修改没有硬上限，补生成不增加业务轮次。
- 无新增迁移，沿用 `0003_generation`；没有修改 `.env`、删除数据、初始化 Git、提交或部署。

### 实际验证 ✅

- 后端 77 项、前端 7 项、Ruff/格式、ESLint、TypeScript、pip check、Next.js 独立目录生产构建通过；Alembic check 无差异。两条已有第三方弃用提示保留。
- 隔离 Chrome 页面完成 5 个初稿场景及 1 次四图输入单图修改；桌面双图正常，390px 修改表单无横向溢出，视口恢复。刷新后修改记录仍存在，切回初稿入选状态保留。
- **本次真实供应商 POST 0 次**。14 次本地模拟 submit、6 个成功任务组、11 张 512×512 模拟结果；不代表模型人物/风格/修改效果验收。
- 23 个本次静态产物/日志/报告文件密钥扫描命中 0。详情见 [阶段 4 验收](docs/stage4-validation.md)。

### 环境、数据与收尾

- 使用并验证原 `E:\2-AI\AIFACE\.venv\Scripts\python.exe`，无安装依赖、无重建环境。
- 默认库只读查得 1 笔业务订单、1 组 succeeded 初稿和 2 个成功任务；未参与本次模拟测试。与前次归档相比已有业务生成结果，不要重新生成或拿来作测试。
- 测试目录 `storage/qa-browser-20260921`；manifest.json 保存场景订单 ID，report.json 保存任务组/计数，simulated-submits.jsonl 为模拟调用记录。脚本 `scripts/browser-qa.py` 可复用，既有目录不重新创建场景单。
- 隔离前端自动启动被审批拒绝（仅 blocked by policy）；用户手动启动 13000 后完成验收。18000 为本次独立模拟后端，生产应用不导入模拟供应商。
- 本次隔离 13000/18000 服务已停止，测试文件及 `.next-browser-qa`、`.next-stage4-build` 保留；Next.js 类型引用与 tsconfig 已恢复默认 `.next/dev`，恢复后类型和整体 lint 通过。
- 开工时默认 3000/8000 与 Worker 在运行，未对它们发停止命令；收尾检查它们已不在运行（原因未核实）。下次先检查进程和端口，再按 README 启动，不能假定后台常驻。没有待执行的本次测试或真实远端任务。

### 下一步 ⏳

阶段 5：最终交付选择/撤销、单图与 ZIP 下载、完成/关闭及终态约束。先读数据/API/架构与前端规则，新增交付迁移；审阅 selected 目前不是最终交付记录，不混为一谈。

阶段 6 仍需真人/真实修改效果、供应商费用和幂等有效期验证；不要为了恢复上下文重复真实探测。若要新增真实联调，先说明目的及预期 POST 次数。submitted_attempt_count 仅表示进入提交阶段的尝试数，不等于真实 HTTP 到达量或账单。

接续口令：**继续 AIFACE，读取 AIFACE_PLAN.md，从阶段 5 开始。**

---

## 阶段 3 历史交接（以下不是最新状态）

### 阶段 3 核心实现和真实联调通过

归档更新：2026-09-21。阶段 3 开发及验收执行于 2026-09-20，随后用户询问四次 API 请求的原因，现要求“记住当前状态，我清空对话”。本节覆盖下方所有历史交接。

### 本次清空前归档

- 本次仅更新交接与验收文档，没有继续开发、重跑测试、调用供应商或启动/停止服务。
- 下方测试、订单数量和服务状态均为上一开发阶段最后检查的记录，归档时没有重新检查。新会话先只读核实实际状态，尤其是默认 Worker 是否仍运行以及是否出现用户新建任务。
- 无本次开发遗留的测试、安装或构建任务需要接管；两笔专用真实远端生成任务此前均已成功结束。默认 Worker 为留给商家使用的后台服务，不应误当作遗留测试进程停止。

### 四次真实 API 提交：已向用户解释

本次真实联调实际向 `/v1/images/edits` 发出了 **4 次 POST**，不能只报告“2 次生成”而省略额外请求：

1. 初稿候选位置 1 的生成提交。
2. 初稿候选位置 2 的生成提交。
3. 重用位置 1 的幂等键和相同正文，验证重放行为。
4. 重用位置 1 的幂等键，但改变提示词，验证冲突行为。

后两次返回原远端任务编号，没有新增任务编号，但**没有核实供应商是否额外计费**。正常一次初稿操作提交 2 次生成请求；查询任务的 GET、图片下载请求另外计算。两次幂等探测为本次额外联调，不属于页面正常生成流程，也不会随每次生成自动执行。

沟通教训：区分“实际 HTTP 提交次数”“远端任务数量”“本地尝试记录”和“账单计费次数”；此前只强调两个生成尝试，已向用户承认表述不完整并道歉。后续报告须明确列出额外验证提交及费用是否核实。新会话不要为恢复上下文重复已完成的真实探测；如有必要新增真实联调，先说明具体目的和预期提交次数，再按授权范围执行。

### 当前完成 ✅

- `0003_generation` 新迁移：任务组、子任务、恢复幂等表、资产任务外键、每单最多一组未结束任务的部分唯一索引；既有资产额度触发器保留。
- 同一写事务重新校验有序输入、来源、状态及真实文件摘要；创建两个独立候选位置，保存完整配置/风格/提示词/输入快照。
- 初稿、任务查询与历史、明确失败补生成、原任务下载/落盘恢复、三种人工核对 API 和前端入口。
- 独立 `python -m app.worker`，每数据库进程锁；短事务领取、HTTP 不占写锁、只读查询退避。无远端 ID 的提交中断进入 submission_unknown，不自动重发。
- 初稿页面轮询、两个候选展示、尝试历史、风险确认；sessionStorage 保存未确认请求的原始正文和幂等键。修改/选择/交付没有提前实施。
- 启动脚本正常模式启动 Worker，SmokeTest 模式不处理付费任务；Python HTTPX 已从仅开发用途纳入运行依赖，原锁文件已含该版本。

### 验证与真实联调 ✅

- 后端 72 项、前端 7 项测试，Ruff/格式、ESLint、TypeScript、pip check、生产构建通过；Alembic check 无差异。
- 旧阶段 2 含素材数据库升级保留、重复提交并发、部分失败、下载/磁盘失败恢复、远端编号重启续查和核对幂等均测试通过。
- 专用程序绘图四张 Base64 输入，两个真实异步编辑任务均 succeeded；得到两张独立 2880×2880 PNG（6,081,344 / 5,930,444 字节），本地 HTTP 内容与文件完全一致。
- 同供应商幂等键重放返回原任务；改 prompt 但同键仍返回原任务而不是报冲突。因此本地请求摘要冲突检查不能去掉；供应商有效期及计费仍未知。
- 初次下载因本机代理把 `img.ksidc.icu` 解析到 198.18/15 被拒绝。复现测试后仅对该精确 CDN/网段加例外，恢复原任务下载成功，没有新建生成尝试。不要放开任意私网/域名。
- 43 个浏览器静态产物、日志和报告检查，本地密钥命中 0；没有修改 `.env`。
- 浏览器在现有服务只读验证生成入口和空记录的桌面/390px 布局，恢复原尺寸及订单列表，没有点击业务单生成。

### 环境与保留数据

- 解释器仍为 `E:\2-AI\AIFACE\.venv\Scripts\python.exe`，首次已验证路径、prefix、中文输出；无 Git 初始化、提交或部署。
- 默认数据库本次发现已有 **1 笔业务订单**（不再是历史记录中的 0）。已做 SQLite 在线备份 `storage/database/before-stage3-20260920-153356.sqlite3` 再迁移，原订单/照片保留，未参与真实测试。
- 原有 3000/8000 服务保留，均可访问；本次额外启动默认数据库 Worker，供页面后续手动生成使用。下一会话先检查 Worker 进程和数据库锁，不重复启动，不假设当前进程长期存在。
- 测试素材/数据库/报告：`storage/qa-stage3-20260920`，前端构建目录 `.next-stage3-qa`，均保留。真实测试任务已结束，无待接管远端任务。
- 独立后端 18000 已停止，13000 前端未成功启动。Next.js 类型文件与 tsconfig 已恢复默认 `.next` 引用。

### 尚未验收与下一步 ⏳

- **阶段 3 完整浏览器生成、部分失败和核对表单交互仍待补测。** 隔离前端服务启动被自动审批拒绝，仅返回 blocked by policy，没有具体原因；未绕过。不能把单元测试、构建或入口布局检查当成整段浏览器验收。
- 真实程序绘图不代表真人效果：输出加了测试原图不存在的眼镜；人物、局部来源及风格质量留阶段 6 人工评估，不擅自改已确认参数语义。
- 下一开发阶段是阶段 4：候选基础图修改、一次一张、基础图计入 4 张、选图/作废、版本及修改轮次。先读相关设计和前端规则，同时补上阶段 3 浏览器验收缺口。
- 接续口令：**继续 AIFACE，读取 AIFACE_PLAN.md，补阶段 3 浏览器验收并开始阶段 4。**
- 详见 [阶段 3 验收](docs/stage3-validation.md)、[实施计划](docs/implementation-plan.md)、[API](docs/api.md)。

---

## 阶段 2 历史交接（以下不是当前状态）

历史日期：2026-09-20。**本节及后文“阶段 1 历史交接”仅供追溯；当前状态以文件顶部阶段 3 归档为准。**

用户本次指令为“继续 AIFACE，读取 AIFACE_PLAN.md，从阶段 2 开始”，已完成阶段 2，未启动阶段 3 或真实模型调用。

### 清空对话前归档

用户随后要求“记住当前状态，我清空对话”。已将本节作为当前交接状态，记忆入口同步到 `memory/MEMORY.md`。本次归档仅更新文档，不继续开发、不重新测试、不启动或停止服务、不调用模型。

新会话接续口令：**继续 AIFACE，读取 AIFACE_PLAN.md，从阶段 3 开始。**

接续时优先阅读本文件顶部最新状态和对应设计，不按后文阶段 1 历史停点重复实施。项目解释器为 `E:\2-AI\AIFACE\.venv\Scripts\python.exe`；首次运行前验证解释器/prefix，保留现有环境和 `.env`。上一开发阶段结束时 3000/8000 原有服务仍在、13000/18000 验收服务已停止；新会话重新检查运行状态，不假定进程持续存在。没有尚在执行的本次测试、安装或构建任务需要接管。

归档时已完成默认 Next.js 类型路径恢复；默认业务订单数最后检查为 0。27 个浏览器产物/日志密钥扫描命中 0，9 份文档编码、链接、围栏及 JSON 示例检查通过。这些是上一开发阶段的实际结果，本次归档未重新执行。

### 已完成 ✅

- 新增订单/素材模型和迁移 `0002_orders_assets`；原 `0001_baseline` 未改。默认数据库已升级，Alembic check 无结构差异。
- 订单创建、客户/编号搜索、状态筛选、分页、详情、基本信息更新。
- JPEG/PNG/WebP 上传，真实格式和完整解码校验，单张 20 MiB / 4000 万像素，拒绝动画和多帧。中文名称可显示，落盘名称使用 UUID。
- 当前素材最多 4 张；主照片、参考图各最多 1 张。SQLite `BEGIN IMMEDIATE` 串行写事务、部分唯一索引与数量触发器兜底，切换主照片原子降级旧主照片。
- 移出/恢复素材、受控图片访问；移出只改标记，失效来源 ID 清空，原文件保留。恢复不自动恢复来源。
- 固定风格 `styles/q_crayon_001/style.json`，参数校验及单图提示词组装。发型必须明确选择本人主照片或辅助图；服装分 person/reference/simplified。
- `POST /orders/{id}/ready` 和只读 `prompt-preview`；后者按显式输入顺序引用图片，跨单/重复/缺失来源/角色不符被拒绝，不自动添加示例图。
- `/orders`、`/orders/new`、`/orders/[id]` 页面及首页入口。可保存有效待生成订单；生成入口明确尚未开放。
- API、数据、架构、README、实施与验收文档已同步，另有 `docs/stage2-browser-tests.md`。

### 验证与环境

- 使用原项目 `.venv`，后端 44 项、前端 6 项测试通过；Ruff/格式、ESLint、TypeScript、pip check 和生产构建通过。
- 覆盖并发上传竞争最后名额、恢复额度、角色唯一、跨单引用、源图须进入实际输入、参数零/空值、中文文件名、图片真实内容、路径越界、终态只读及应用重启保留业务数据。
- 浏览器已检查中文订单创建、参数保存、待生成、预览、移出恢复、搜索、重新打开、错误页；桌面及 390px 窄屏布局无明显裁切，浏览器尺寸已恢复。
- Chrome 自动文件选择受扩展的文件 URL 权限限制；上传使用同源 HTTP 验证，未声称浏览器文件选择到提交整段验收通过。
- 13,250,802 字节 PNG 同源上传 201 且读取内容相同；20 MiB+1 返回 413。曾复现 Next.js 默认 10 MiB 缓冲截断，已调到 22 MiB。
- 新依赖 Pillow 12.2.0、python-multipart 0.0.32，已安装并更新锁文件。12.3.0 下载不完整且哈希失败，未绕过校验。
- 默认数据库：`storage/database/aiface.sqlite3`；素材根目录默认 `storage`，可用 `AIFACE_STORAGE_PATH` 隔离。
- 本次发现用户已有 3000/8000 服务，已保留，迁移后两端健康检查均为 200。本次隔离验收使用 13000/18000，两服务现已停止。
- 隔离测试数据保留：`storage/qa-stage2-20260920`。前端测试构建目录 `.next-stage2-qa`，未执行物理清理。
- 前端服务端环境可设置 `AIFACE_BACKEND_ORIGIN` / `AIFACE_NEXT_DIST_DIR`，默认目标仍为 8000 / `.next`。自定义目录留在 frontend 内，使用 `.next-*` 以匹配 Git/ESLint 忽略规则。
- 未修改 `.env`、未初始化 Git/提交、未部署、未调用供应商。

### 下一步：阶段 3 模型任务闭环 ⏳

先读 `docs/implementation-plan.md`、`docs/data-model.md`、`docs/api.md`、`docs/architecture.md` 和 `image2.md`，前端修改继续遵循 `frontend/AGENTS.md`。

1. 新迁移建立 generation_batches、generation_tasks；补 assets.generation_task_id 外键（目前仅可空唯一预留）。
2. 在同一写事务中重用阶段 2 的参数/有序输入预检，并验证文件可读，建立初稿两个独立位置、完整不可变快照与本地幂等记录。
3. 实现供应商适配、独立 Worker 轮询、落盘及恢复；先模拟，后少量真实测试。
4. 区分明确失败、查询失败、下载失败、提交结果不明，后者无远端 ID 时不得自动重发。
5. 实现任务展示、部分成功和失败补生成；已有成功结果保留，同订单最多一个未结束任务组。
6. 验证供应商四图格式、单图响应、异步状态与幂等语义；不能用阶段 2 的本地预检替代真实生成验收。

初稿固定 2 张、每次修改 1 张、输入最多 4 张、只记录修改轮次而无硬上限等原有确认继续有效。缺密钥可启动，但调用前执行 `Settings.require_image_api()`。`prompt-preview` 只读、没有任务快照，阶段 3 提交必须重新检查，不能信任之前的预览结果。

### 本次新注意项

- 修改角色也可能使来源失效，不能只在移出时清空来源。
- 未保存参数会阻止素材操作；已补撤销按钮，来源不齐时可撤销后补图，不让表单卡住。
- 自定义 Next.js 构建目录须同时加入 Git/ESLint 忽略规则；Next 会自动修改 tsconfig/next-env 引用，验收后恢复默认生成路径。
- 不自动删除孤立图片、临时文件或测试数据库；维护清理仍需用户确认具体范围。
- 不停止用户原有服务来腾出端口；使用独立测试端口与数据目录。

---

## 阶段 1 历史交接（以下为旧记录）

归档日期：2026-09-20。项目根目录：`E:\2-AI\AIFACE`。

## 1. 当前停点

用户要求先按 `docs` 计划创建项目目录和虚拟环境，随后要求继续，已完成阶段 0（规划）和阶段 1（基础工程）。用户现在要求归档会话，准备清空对话、开启新会话。

**目前基础框架可运行，业务功能尚未实现。下一步从阶段 2“订单、风格与素材”开始，不能把首页可打开视为接单工作台已经完成。**

归档本身只写交接文档，不继续开发，不启动服务，不调用模型。新会话用户说“继续 AIFACE”时，读取本文件和相关设计后接续阶段 2，无需重建目录、虚拟环境或重复实施阶段 1。

## 2. 阅读顺序与依据

1. [实施计划](docs/implementation-plan.md)：阶段进度和后续任务。
2. [架构设计](docs/architecture.md)：目录、分层、运行约定。
3. [数据模型](docs/data-model.md)：业务表、事务约束及状态流转。
4. [API 契约](docs/api.md)：订单、图片、生成、交付协议。
5. [验收清单](docs/acceptance.md)：基础工程的实际验证与尚未执行的业务验收。
6. [README](README.md)：依赖恢复、启动、检查、停止、备份。
7. [产品原方案](头像.MD) 与 [供应商模型接口](image2.md)：原始依据；不要混淆两个文件。

修改前端前还需读取 [前端 AGENTS.md](frontend/AGENTS.md)，并依其要求阅读 `frontend/node_modules/next/dist/docs/` 中与任务有关的指南。该 AGENTS.md 和 CLAUDE.md 由 Next.js 开发服务器自动生成，不要当成无关垃圾删除。

## 3. 已确认 ✅

### 用户业务要求

- 商家内部头像接单工作台，首版单风格。
- 输入图片最多 4 张。
- 初稿返回 2 张独立候选图。
- 基于候选图修改，每次返回 1 张。
- 只记录修改轮次，由商家控制，不设置系统硬上限，不自动循环返工。
- 根目录已有 `.env`，`image_api` 非空；未验证有效性，没有修改或公开密钥。

原产品方案中的默认 4 张候选等历史建议已被上述数量要求覆盖。不要再次让用户确认已经明确的规则。

### 已完成的基础工程

- 静态目录骨架：frontend、backend、styles、storage、scripts 及规划子目录。
- 根目录 `.venv`、后端依赖和前端依赖；`.gitignore`、`.env.example`、README。
- `backend/app/config.py`：从项目根目录 UTF-8 `.env` 和环境变量读配置；密钥脱敏；数据库相对路径固定基于项目根目录。
- `backend/app/db.py`：SQLAlchemy 引擎、声明基类、事务会话；SQLite 外键、WAL、5 秒锁等待。
- `backend/app/main.py`：`create_app` 工厂入口、lifespan、健康检查、统一脱敏错误及请求关联 ID。
- Alembic 配置、迁移环境、模板和 `0001_baseline` 基线。
- `frontend/src/app/`：首页、布局、CSS；首页为工作室基础展示，订单区明确标注尚未开放，插画是 CSS 示意图。
- `frontend/src/components/service-status.tsx`：连接状态、超时提示和手动重试。
- `frontend/src/lib/api.ts`：同源 GET 请求、错误消息、请求 ID 和取消信号。
- `scripts/start-dev.ps1`：先迁移，启动前后端，检查就绪，Ctrl+C 停止本次进程树，支持 `-SmokeTest`。
- `scripts/check.ps1`：后端 lint/格式/测试/依赖检查和前端 lint/类型/测试，可选 `-Build`。

### 实际验证结果

以下是上一开发阶段的实际结果，本次归档未重复运行：

- 后端 14 项、前端 4 项测试通过。
- Ruff、Ruff format、ESLint、TypeScript、pip check 通过。
- Next.js 生产构建通过；npm 安装审计为 0 个已知漏洞（当次检查结果）。
- 迁移重复执行保留已有数据，外键及事务回滚通过。
- 无模型密钥可运行基础工程；未迁移数据库健康接口返回 503。
- 首页与同源 `/api/health` 实际启动验证通过。
- Chrome 桌面及 390px 窄屏检查通过，显示“工作台已连接”，创作流程锚点可用。
- 对 20 个浏览器静态产物及运行日志文件匹配本地密钥，未检出泄漏；未输出密钥。
- 上次开发结束已停止测试服务，并验证 3000、8000 端口释放；新会话需要运行时重新检查端口，不假设后台服务仍在。

## 4. 环境与启动方式

| 项目 | 当前配置 |
| --- | --- |
| Shell | Windows PowerShell |
| Python | `E:\2-AI\AIFACE\.venv\Scripts\python.exe`，3.12.14 |
| pip | 项目环境内 25.0.1 |
| Node.js / npm | 22.23.2 / 10.9.8 |
| 前端 | Next.js 16.3.5、React 19.3.0、Tailwind CSS 4.3.3、TypeScript 6.0.3 |
| 前端 lint | ESLint 9.39.5，暂不升级到 10，见下方已知问题 |
| 后端 | FastAPI 0.141.1、SQLAlchemy 2.0.54、Alembic 1.20.0、Pydantic Settings 2.15.0 |
| 锁定依据 | `backend/requirements.lock.txt`、`frontend/package-lock.json` |
| SQLite | 默认 `storage/database/aiface.sqlite3`，目前仅有 `alembic_version`，无业务表 |
| 运行日志 | `storage/logs/`，按每次启动独立命名 |
| Git | 尚未初始化仓库，也没有本次任务提交 |

在项目根目录执行：

```powershell
# 启动开发环境；按 Ctrl+C 停止。
.\scripts\start-dev.ps1

# 基础检查，或带生产构建的完整检查。
.\scripts\check.ps1
.\scripts\check.ps1 -Build

# 自动启动、检查首页和同源 API，再停止。
.\scripts\start-dev.ps1 -SmokeTest

# 单独执行数据库迁移。
.\.venv\Scripts\python.exe -m alembic -c backend/alembic.ini upgrade head
```

页面地址 `http://127.0.0.1:3000`；后端 `http://127.0.0.1:8000`；API 文档 `/api/docs`，OpenAPI `/api/openapi.json`。

`GET /api/health` 成功返回 `{"status":"ok","database":"ready","version":"0.1.0"}`。前端通过 Next.js rewrite 将同源 `/api` 转发到本机后端，不读取根目录密钥配置。

后续 Python/pip/pytest/Ruff 均使用上述项目解释器。新会话首次运行前检查解释器及 prefix，不重建或跨项目复用环境。需要恢复依赖时按 README 执行，不向全局 Python 安装依赖。

## 5. 关键实现决策

以下属于已采用的实施方案，业务规则未变化时据此继续；不把每一项描述为用户逐条确认。

- Next.js + FastAPI + SQLite + 本地图片存储，首版单机本地使用。
- 独立 Worker + 数据库轮询，不引入 Redis/Celery；Worker 尚未实现。
- 初稿拆为两个单图子任务，修改为一个单图子任务。供应商文档没有批量 `n` 参数，不自行假设支持。
- 首版固定 `q_crayon_001`、白底、1:1；风格配置和提示词模块在阶段 2 实施。
- 单次实际模型输入同样最多 4 张；修改基础图和明确选入的风格参考图都占额度。
- 当前订单素材最多 4 张、主照片最多一张、参考图最多一张；草稿可无主照片，生成前必须恰好一张主照片。上传与角色调整须用事务防并发绕过。
- 风格示例不自动附加；修改时不能在基础图之外自动携带全部原素材。
- 一个订单同时只允许一个未结束任务组；结果不明也阻止新生成，避免重复付费。
- 任务快照与当前可编辑参数分离，失败重试保留历史和已成功图片。
- 移出素材、作废结果只改标记，不物理删除。移出素材需清空受影响的当前来源参数，历史引用保留。
- 最终图可选一张或多张；下载与标记订单完成是两个动作；已完成/关闭订单首版只读。
- 缺密钥可启动基础工程，需要模型调用时使用 `Settings.require_image_api()` 明确检查。
- 供应商文档 Base URL 为 `https://ai.apii.cn`，模型名 `gpt-image-2.0-4k`；1:1 文档尺寸为 2880×2880，不能声称输出必为 4096×4096。
- 查询失败、下载失败、明确生成失败、提交结果不明必须区别处理；无远端 ID 的提交超时不能自动重发。
- 修改轮次按新修改任务组计数，补生成不增加修改轮次；费用未知为空，不显示成零。

## 6. 待实现与待确认 ⏳

### 下一步：阶段 2 订单、风格与素材

目标：能够创建订单、上传整理素材并保存有效参数，准备一笔待生成订单。

建议按不超过 3 个文件的小批次逐步完成：

1. 先读数据/API/架构文档，核对当前代码和前端规则。
2. 实现订单及图片业务模型、Alembic 新迁移和必要测试；不要修改已完成的迁移基线来冒充业务迁移。
3. 实现订单创建、分页列表、详情及基本信息更新。
4. 实现上传、角色切换、移出/恢复当前素材及受控图片访问。
5. 加载固定风格配置，实现当前参数校验、来源引用和提示词组装。
6. 实现订单列表、新建、详情及素材界面，对接真实本地 API。
7. 逐模块验证并同步设计/API/验收/实施进度。

关键验收：无主照片不得生成，第 5 张素材被拒绝，参考图最多一张，图片来源不串单，引用来源必须进入实际输入，并发上传不能绕过限制。

### 后续阶段

- 阶段 3：任务组/子任务/快照、供应商适配、Worker、幂等和失败恢复；先模拟测试再少量真实联调。
- 阶段 4：基于候选图修改、选图/作废、版本记录和轮次统计。
- 阶段 5：最终图选择、图片/ZIP 下载、完成/关闭。
- 阶段 6：全流程、异常恢复和 10～20 笔真实或模拟订单试用。

### 对应阶段再核实的外部问题

- 供应商是否支持所用 Base64 多图格式和 4 张输入。
- 单次响应图片数量、异步状态、幂等键有效期/冲突/重放语义。
- 上传限制暂定 JPEG/PNG/WebP、20 MiB、4000 万像素，需联调核对供应商约束。
- 真实测试素材、人物保持、风格效果、真实尺寸和费用依据。

上述问题尚无验证结果，不阻止阶段 2 的本地开发，也不应让用户重新确认已确定的出图数量规则。

## 7. 踩过的坑与限制

- Windows PowerShell 5.1 会将无 BOM 的中文 `.ps1` 按 ANSI 解码，引发乱码甚至语法错误；现有两个脚本已改为 UTF-8 BOM，后续编辑必须保留。
- PowerShell 向 Python 管道传中文脚本时，应先设置 `$OutputEncoding = [System.Text.UTF8Encoding]::new($false)`。只设置 Python stdout 编码不能修复输入阶段的乱码。
- Next.js 16.3.5 携带的 React lint 插件在 ESLint 10.11.0 下触发 `contextOrFilename.getFilename is not a function`。回到 9.39.5 后 lint 通过；该旧版 npm 提示停止维护，等待上游兼容后再评估升级，不要盲目升级。
- 后端测试目前有 Starlette/httpx、AnyIO 两条第三方弃用提示，未隐藏，暂不影响通过。
- Next.js 页面内跳转应使用 `next/link`，曾由 lint 检出首页普通 `<a href="/">` 并已修复。
- `frontend/AGENTS.md`、`frontend/CLAUDE.md` 由 `next dev` 自动生成；最后检查时已读取，后续前端修改须遵循本地指南。
- SQLite 停止后可能保留 WAL/SHM 文件；备份按 README 先停服务再复制完整 storage，不擅自删除。
- 不把阶段 1 的 18 项基础测试等同业务验收通过；目前不能创建订单、上传图片或生成头像。

## 8. 用户工作规则与接续纪律

- 所有回复及代码注释使用中文，变量/函数/文件名保持英文。
- 用户问“为什么/怎么工作”时先文字解释；未明确要求操作时不调用工具。
- 编码前简短说明方案，然后自动执行普通任务，不重复问“是否继续/批准”。业务逻辑真有不确定处再澄清。
- 删除、清空、不可恢复覆盖、丢弃修改、强制推送等危险操作需具体说明并人工确认。当前任务没有清理授权。
- 超过 3 个文件的任务先拆分；逐模块 lint 和运行受影响测试，不等全部改完再验证。Bug 修复先有可重现测试，再修复。
- 修改前先读对应设计，修改后同步受影响文档和变更记录。
- 文件读写显式 UTF-8；会输出中文的 Python 命令脚本按全局要求设置 stdout/stderr，捕获具体异常。
- 遵守当时有效的开发者多 Agent 限制；本会话未启动子 Agent，新会话不要自行推定有并行代理授权。
- 本次没有 Git 初始化、提交、真实模型调用或部署；不擅自增加发布步骤。
- 按用户要求先归档再清上下文；归档完成后由用户清空会话。

## 9. 交接检查

- 已将本文件入口写入 [memory/MEMORY.md](memory/MEMORY.md)。
- 完成状态、测试证据、未完成范围和下一步已记录；本次无正在进行的代码修改或测试进程需要接管。
- 新会话接续口令：**继续 AIFACE，读取 AIFACE_PLAN.md，从阶段 2 开始。**

## 2026-09-21 固定素材命令与精简工作区

已确认：逐项修改改为额外提示词描述；素材使用固定编号，由提示词指明用途。左侧只保留订单信息、主图片、风格下拉、素材上传、额外提示词；结果与连续修改展示在右侧，取消回跳。素材位置1～3保留空位，不推断固定语义用途。风格示意图保留在风格库，订单内不展示。

实现：material_slots 参数及事务上传/替换/移出接口；兼容历史结构化快照；右侧输出比例；上传时保留文字草稿，继续修改持久化实际底图但左侧仍显示原始主图片。验收见 docs/compact-workspace-validation.md。
