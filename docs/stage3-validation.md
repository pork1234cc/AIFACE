# 阶段 3 验收记录

执行日期：2026-09-20；计数说明补记：2026-09-21。以下保存原阶段 3 记录。**2026-09-21 已用隔离模拟供应商补齐浏览器双图生成、部分失败补生成及三种核对表单，见 [阶段 4 验收](stage4-validation.md)。本次补测真实供应商提交为 0。** 下文“待补”属于补测前历史状态。

## 自动验证

最终已通过后端 72 项、前端 7 项；Ruff/格式、ESLint、TypeScript、pip check 和 Next.js 生产构建通过。第三方 Starlette/httpx 与 AnyIO 的两条弃用提示保留，未屏蔽。

- 后端覆盖生成快照、本地幂等冲突、并发相同请求、单个未结束任务组、两张落盘、部分失败补生成、原任务下载/落盘恢复、Worker 进程锁、提交中断不自动重发、远端编号重启续查、核对及风险确认、旧数据库迁移保留与 FK 检查。
- 前端包含幂等头保留的 API 测试，ESLint、TypeScript 和独立目录生产构建通过。行为单测不能代替浏览器恢复表单验收。
- 迁移 `0003_generation` 已在默认数据库应用；迁移前在线备份为 `storage/database/before-stage3-20260920-153356.sqlite3`，原有 1 笔订单保留，Alembic check 无结构差异。

## 真实供应商联调

全部使用程序绘制的虚构头像，共四张输入（主照片、两张辅助、参考图），无业务照片。存储和数据库位于 `storage/qa-stage3-20260920`，详细脱敏结果见该目录 `report.json`。

- 实际共 **4 次 `/v1/images/edits` POST**：两个独立初稿提交，另加一次同键同正文重放和一次同键改提示词冲突探测。images 为四个 Base64 字符串，async=true，没有 n 参数。任务查询 GET 和图片下载请求不计入这 4 次。
- 两任务均返回有效 task_id，观察到 running，最终 succeeded，每个成功结果恰好一张 URL 图片。
- 初稿输出两张 PNG，均 2880×2880，分别 6,081,344 和 5,930,444 字节；已完整解码、SHA-256 记录并通过本地图片接口读取。
- 同键相同正文重放返回原 task_id；同键不同 prompt 也返回原 task_id，没有返回冲突。应用本地摘要冲突仍严格 409。未新增第三个远端编号。
- 本次只验证约数分钟内的重放，不知道供应商幂等有效期，也没有价格/计费凭据；费用字段为空，不声称免费。
- 后两次验证提交没有新增远端编号，不等于已证明未扣费。正常页面一次初稿只发起两个生成提交，不自动执行这两次探测。用户已就“四次 API 请求”提出询问，已解释并为之前没有区分提交数与生成尝试数道歉；后续应分别报告请求数、任务数及实际计费依据。
- 初次下载被代理伪 IP 拦截。先编写失败测试复现，再仅允许已观察到的 CDN `img.ksidc.icu` 的 198.18/15 伪 IP；继续拒绝其它域名和私网。恢复原任务下载后成功，生成尝试仍为 2。
- 两张结果均为独立蜡笔风格头像，非拼图；人工查看发现测试原图没有眼镜，但结果有眼镜。当前可证明协议闭环，不等同人物细节/真实客户效果验收，阶段 6 需继续检查。

## 浏览器及运行限制

- 保留用户原有 3000/8000 服务，默认业务素材未参与真实调用。
- 已在现有 3000 页面只读确认新增“初稿与生成记录”、有序素材列表、“生成两张初稿”、刷新和空记录提示，桌面及 390px 窄屏无明显裁切；未点击业务订单生成，浏览器尺寸和原订单列表已恢复。
- 独立 13000 前端启动被自动审批拒绝，工具仅返回 blocked by policy，没有提供具体原因。独立 18000 后端可运行，已用于 HTTP 恢复和图片读取。
- 因此，包含真实结果、部分失败和核对表单的完整浏览器流程仍待补测；自动测试及生产构建通过不能替代这项验收。
- 验收结束停止本次 18000 后端，保留测试文件和构建目录；新增默认数据库 Worker 供商家页面手动生成，未触发原业务单调用。默认 3000/8000 健康，前端类型引用恢复 `.next`。43 个浏览器静态产物、日志和报告密钥扫描命中 0。

## 待核实

- 供应商幂等有效期、并发和计费依据。
- 真人特征、眼镜和指定来源的视觉保持；真实素材须按用户授权使用。
- 新增任务/恢复界面的完整浏览器交互验收。
- 修改、选择、交付属于阶段 4～5，未提前实现。
