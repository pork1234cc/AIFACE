# 阶段 3 浏览器补测与阶段 4 验收

日期：2026-09-21。阶段 3 的生成、部分失败、三种核对表单缺口已补齐；阶段 4 修改与审阅实现及模拟验收通过。没有进行本次真实供应商提交，真人效果与真实修改效果不在本次结果内。

## 环境与数据

- 项目 `.venv/Scripts/python.exe`，已检查解释器、prefix 和中文输出。
- 开工时原 3000/8000 与默认 Worker 在运行，本次未向这些默认进程发停止命令；默认库只读检查为 1 笔业务订单、1 组已成功初稿、2 个成功任务，没有对该订单执行本次验收操作。
- 隔离 13000/18000，数据目录 `storage/qa-browser-20260921`；场景订单 ID 见 manifest.json，任务组和结果计数见 report.json。
- 13000 的自动启动被审批拒绝，仅返回 blocked by policy；用户按给出的命令手动启动后完成浏览器补测，没有绕过限制。
- `scripts/browser-qa.py` 仅在隔离进程使用本地模拟供应商，服务配置为 `Settings(_env_file=None, image_api=None)`；迁移仍复用项目配置读取 `.env`，但不使用其中密钥调用供应商。生成、查询及下载均为本地模拟。测试图片为程序绘制的 512×512 标记图。
- 本次 14 次模拟 submit 调用、6 个成功任务组（5 组初稿、1 组修改）、11 张结果；**真实供应商 POST 为 0**。14 次包含模拟结果不明的提交，不能把它们当作真实账单。

## 浏览器操作证据

使用 Chrome 真实页面点击和填写表单，后端为真实应用/数据库/Worker，供应商为模拟实现。素材预置使用本地 HTTP；本次不声称验收浏览器文件选择器上传。

| 场景 | 实际观察 |
| --- | --- |
| 初稿成功 | 页面点击生成两张初稿，待执行时禁止重复生成，轮询后 2/2 保存；两个本地图片均加载成功 |
| 部分失败 | 1/2 保存，失败位置提供补生成按钮；点击后只增加该位置第 2 次尝试，成功图片 ID 保持不变；历史保留原失败，共 3 次尝试 |
| 关联远端 | 模拟提交不明使新生成禁用；展开核对表单，填写远端 ID 和依据；原任务恢复，最终 2/2、仍只有 2 次尝试 |
| 确认未受理 | 选择未受理、填写依据，旧任务明确失败；人工再点补生成，最终 2/2、3 次尝试 |
| 接受风险重发 | 填写依据但未勾选风险时提交按钮禁用；勾选后生成成功，展开历史可见“已另开尝试，旧结果仍未知” |
| 结果审阅 | 初稿 1 入选；初稿 2 作废后修改按钮禁用、原图仍可见，再恢复待审阅成功 |
| 修改表单 | 基础图默认占 1 张且不自动带素材；指令空时不能提交；明确勾选 3 张后显示 4/4，第 4 张附加素材禁用 |
| 单图修改 | 填写“保留整体风格，去掉眼镜”，生成 1/1；轮次显示 1、成功 1，基础图链接正确、风格版本 1.0.0、输入 4 张 |
| 历史与布局 | 切回初稿显示原 2 张，入选标记保留；桌面双图并排；390px 覆盖下表单/按钮可用，文档内容宽度与滚动宽度均 375px，无横向溢出；已恢复视口 |

页面观察有异步轮询延迟，检查最终状态后才判定通过；工具短等待超时没有触发重复生成。

## 自动检查

- 后端 77 项、前端 7 项通过。
- 新增 5 项修改测试覆盖并发同键、冲突重放、终态重放、单位置输出、后续版本继承、基础图加附加图上限、空白指令、跨单/重复/角色不符/已移出来源、作废基础图、文件缺失、审阅恢复、补生成轮次不变、终态拒绝及 HTTP 契约。
- Ruff/格式、ESLint、TypeScript、pip check 通过；Next.js 独立目录 `.next-stage4-build` 生产构建通过。
- Alembic check 无差异，阶段 4 沿用 `0003_generation`，无需迁移。
- 23 个本次前端静态产物、日志、报告文件扫描，本地密钥命中 0；未修改 `.env`。
- 继续保留 Starlette/httpx 和 AnyIO 两条已有弃用警告。

收尾：停止本次隔离 13000/18000 服务，测试目录和构建产物保留，默认 Next.js 类型引用已恢复并通过类型/整体 lint。额外单独 lint 生成文件 next-env.d.ts 时工具提示该文件被忽略，随后使用项目整体 lint 正常通过。默认 3000/8000 和 Worker 收尾已不在运行，原因未核实；本次停止命令仅针对已核实的隔离进程。下次启动前重新检查，不假设默认 Worker 常驻。

## 范围与后续

修改请求和审阅在默认应用已可用，原 Worker 可处理单位置修改，无需为本次功能更换 Worker。阶段 5 最终交付选择、下载/ZIP、完成/关闭尚未实施；审阅“入选”不是最终交付记录。

模型真实理解“去掉眼镜”、人物保持和风格质量需要后续真实效果验收。本次只证明提示词不再施加旧眼镜控制项，以及应用链路/数量/来源正确。供应商幂等有效期与计费仍未知，不能从 submitted_attempt_count 推断 HTTP 到达量或账单。
