# 阶段 5 单图交付验收

日期：2026-09-21。未初始化 Git；代码标识为本地阶段 5 实现，数据库 head `0005_single_generation`。源文件摘要另存 `storage/qa-stage5-20260921/code-hashes.json`。

## 已确认流程

素材→按风格生成 1 张，直接作为当前交付→按需修改 1 张，成功自动更新、失败保留原图→旧版可切回→下载当前原图→独立确认完成或关闭。历史双图保留，不自动替旧订单选图；新流程不提供多图 ZIP。

## 自动检查

后端 88 项、前端 10 项通过；Ruff/格式、ESLint、TypeScript、pip check、Alembic check 和 `.next-stage5-build` 生产构建通过。沿用项目 `.venv`，两条已有第三方弃用提示未隐藏。

覆盖单图目标/实际单次 submit、修改失败保留/重试成功替换、切回旧版、同版本幂等、跨单/作废/多图拒绝、并发唯一当前图、下载字节及文件名、导出期间换图冲突、文件缺失不记导出、完成/生成竞态、终态幂等和下载。迁移测试从含旧双图/资产外键的 0003 升级两次并核验外键及素材触发器。

末轮自检发现：页面已禁用版本切换，但 API 未拒绝有未结束任务时撤销选择。新增 pending/running/needs_attention 三种复现断言，先观察 200 而非 409，再在 PUT 写事务内加 ensure_available；交付模块 8 项回归通过。前端下载测试另外覆盖危险文件名拒绝。

## Chrome 实际验收

隔离地址 13000→18000，数据 `storage/qa-stage5-20260921`。前端因先前自动审批拒绝启动，由用户手动启动；没有绕过限制。

- 首次 4 张素材输入，只产生 1 个任务及 1 张当前交付图，无需人工入选。
- 点击“修改这张图”，填写中文指令，仅使用基础图生成 1 张；等待期间保留原图，完成/关闭按钮禁用。
- 成功后自动切为版本 2，版本 1 保留。点击首版“使用此版本交付”，当前图切回版本 1，切换历史共 3 条。
- 点击下载，显示“下载已发起，订单状态未改变”；同源 HTTP 原图字节与资产内容完全相同，9,076 字节，安全订单编号文件名。下载发起已验证，未声称客户收到文件。
- 确认完成后刷新，状态仍为完成；修改/终态操作入口消失，当前图仍可下载。
- 第二笔隔离订单无结果时完成按钮禁用，确认关闭成功；保留素材和记录。
- 390×844 视口的 documentElement clientWidth/scrollWidth 均为 375（滚动条占宽），当前图片 naturalWidth=512；截图中版本卡片和确认区可见，无横向溢出。视口恢复，测试标签关闭；测试页 error 控制台记录为空。

主订单 `c5a5c6ce-b9a5-4558-be35-86a934e28d8b`，首次组 `2781b5d2-85ee-479a-8618-74a898192b58`，修改组 `ed7d808e-9f7e-46f8-b6ae-6d7aa3e7be8d`。两个任务组均 target_count=1、succeeded。闭单测试订单 `1b0a8007-589a-475b-bb50-9d39e31f927d`。manifest 场景标签继承旧脚本；实际主订单名已改为“模拟验收-单图交付”。

## 数据与环境

默认库先 SQLite 在线备份至 `storage/database/before-stage5-20260921-005105.sqlite3` 后升级。原 1 笔业务订单、旧双图成功任务及资产逐字段不变；未选交付图、未发起新业务生成。外键及 Alembic 元数据比较通过。

验收完成后仅停止本次隔离服务，13000/18000 已释放；默认 3000/8000 开工即未运行，未对默认进程发停止命令。Next.js 类型路径恢复默认 `.next`，测试数据/日志/构建目录均保留。

## 调用与限制

**本次真实供应商 POST 0 次；本地模拟 submit 2 次，分别是首次生成与修改。** 两张 512×512 模拟图仅验证程序流程，不代表模型风格、人物一致性或修改效果。修改失败保留原图由自动测试覆盖，本次浏览器没有故意制造修改失败。

真实人物与修改效果、10～20 笔整体试用、费用和幂等有效期仍待阶段 6。历史阶段 3 的 4 次真实 POST（2 次生成＋2 次幂等探测）不能与本次模拟调用混算，也不要为了接续重复请求。

最终静态检查：12 份文档 UTF-8、链接、围栏及 JSON 示例通过；65 个源码文件 SHA-256 已归档。62 个本次浏览器静态产物、日志和报告按本地密钥匹配，命中 0；扫描不打印密钥。默认业务表再与迁移前备份逐字段比对一致，交付表仍为空。结果见 `storage/qa-stage5-20260921/final-checks.json`。
