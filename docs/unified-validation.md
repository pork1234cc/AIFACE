# 统一创作流程验收（2026-09-21）

## 已实现

以主照片为底图，支持纯风格转换及明确素材映射；首次/继续修改共用配置，单次一张。新蜡笔风格 2.0.0 取消单人、白底、半身和方形硬限制。新库、新表隔离旧订单；保留新流程的任务恢复、版本历史和只读终态。

## 自动检查

- `scripts/check.ps1 -Build` 完整通过：后端 104 项测试、Ruff 和格式检查、pip check；前端 51 项测试、ESLint、TypeScript、生产构建。
- 生产构建目录 `.next-unified-build`，包含新增 `/styles` 页面。验收后移除本次新增的 TypeScript 构建路径，恢复原默认 `.next/dev/types` 引用。
- 新默认数据库已经初始化到 `0006_unified_creation`，Alembic check 无差异。旧默认库只读复核仍为 2 个 succeeded 任务，未新增 v2 表。
- 单独覆盖旧 pending 任务不被新 Worker 领取、素材引用去重/缺失/跨单/失效/超限、11 种比例、同目标冲突、预览与提交文件校验一致、版本快照与幂等、失败保留原交付、仅恢复下载、历史切换及终态约束。
- 原来针对单人控制项、旧角色、旧双图恢复的断言已改为新业务断言；未增加兼容层来维持旧行为。

## 浏览器实测

隔离前端 13000、模拟后端 18000，数据 `storage/qa-unified-20260921`，结构化记录见该目录 `report.json`。

1. 主工作台无独立创作流程导航；`/styles` 显示唯一内置风格及真实占位提示。
2. 查看基础提示词展示 2.0.0，不改变所选风格。
3. 纯风格提交仅发送一张底图，成功直接显示单张当前交付图。
4. 继续修改明确显示当前结果作为底图，清空上一轮修改项，默认保持原图风格。
5. 填写人物脸部修改项，绑定一张素材，保存 3:4 比例，模拟生成第二版自动更新交付；本轮仅发送底图加引用素材两张。
6. 历史展示对应配置、完整提示词和底图/结果对比。
7. 第三轮未提交的手势修改草稿刷新后恢复，连同新底图 ID 一起恢复；验收草稿最后保存但未生成。
8. 1440px 桌面两栏约四比六；800/390px 上下排列，无横向溢出。页面 console error 为空。

浏览器模拟 submit 2 次、模拟结果 2 张、远端真实任务 0、真实生成 POST 0。未使用真实人物素材，不把模拟图当作风格或身份效果证据。

## 保留的限制与后续

- 真人一致性、多人版式、裁剪和局部替换衔接仍需真实模型效果验收；实施前应列明场景和预计生成 POST 次数。
- 11 种输出比例依据仓库 `image2.md`，本次不宣称全部已经远端实测；模拟图片固定 512×512。
- 费用及幂等有效期未新增外部验证；费用未知继续为空。
- 后端保留两条第三方弃用提示；开发模式有 Next 图片尺寸/LCP 建议，不影响通过的功能验证。
- 本次隔离进程已关闭，视口已恢复，验收数据与构建产物保留；未停止原来的 3000/8000 服务。旧进程正常退出后，使用原启动入口启动新版本。
