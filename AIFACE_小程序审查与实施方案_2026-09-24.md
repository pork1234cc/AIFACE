# AIFACE 微信小程序：产品、UI、界面文案与实施方案

审查日期：2026-09-24（同日按用户确认的个人认证主体与激活兑换码方案修订）  
审查仓库：`pork1234cc/AIFACE`  
固定基线：`31921e21d2fd0056f53011a9f0670819f83a94a8`（读取时 main）  
性质：源码与 HTML 原型静态审查，以及下一阶段实施设计。没有修改远程仓库，没有启动现有应用，没有进行微信真机测试，没有调用付费生图或真实支付。下文新增接口、目录、模型及规则均为建议，不代表仓库已实现。

## 0. 结论与不可改丢的要求

**方向成立：把现有商家工作台的生成能力，包装为消费者能自行操作的头像产品。不是把后台缩窄，也不是把参考图功能砍掉。**

最新 `docs/miniprogram-plan.md` 已明确：优先微信小程序；登录注册；新用户免费生成 3 个头像，之后通过激活兑换码增加次数；首版同时支持主照片生成和参考图编辑；两种模式都必须选择风格。参考图可有多张，数量上限由真实模型能力、效果与成本验证决定。一次输出一张头像、默认单人和 1:1，是文档中的首版建议。[R1]

用户已确认当前为个人认证小程序，无支付功能，新增次数只使用激活兑换码。两种模式共用额度、成功交付一张使用一次仍为建议扣次规则；每码次数、兑换截止时间、到账权益有效期、发放方式、失败补偿期限和多参考图成本需在相应功能开发前定稿。此处记录项目业务约束，不作所有个人主体的平台政策结论。

必须保留的业务语义：主照片决定人物身份与基础画面；参考图只提供用户明确指定的元素；所选风格统一改变绘画表现。所谓“保留未指定内容”，指尽量保留身份、构图和内容关系，不是承诺逐像素不变，更不是默认照搬参考图人物的脸。

本次建议遵循三条主线：

1. 前台用消费者语言表达双模式，照片、参考图用途和风格三类信息分清。
2. 优先复用已存在的生成快照、任务恢复和供应商适配，不从零重写整个引擎。
3. 新增真正的消费者业务层：登录、照片权限隔离、作品、次数账本、兑换码、审核与私有交付。不能只给旧 API 套一个小程序界面。

## 1. 仓库实际状态与关键差异

### 1.1 什么已存在，什么尚未实现

| 层级 | 实际情况 | 对下一步的意义 |
| --- | --- | --- |
| 小程序产品方案 | `docs/miniprogram-plan.md`，明确标注“规划建议，尚未实施” | 是需求基线，不是功能验收记录 |
| UI 草图 | `outputs/avatar-miniprogram-ui.html`，浏览器 HTML/CSS/JS 与模拟状态 | 可用于讨论页面，不等于可发布的微信工程 |
| Web 管理端 | Next.js/React，订单、素材、风格和模型配置 | 保留为经营后台，不直接暴露消费者 |
| 生成引擎 | FastAPI、SQLAlchemy/SQLite、独立 Worker；单图、输入/风格快照、幂等、原任务恢复 | 可以复用，但需要新增消费者交付和结算边界 |
| 消费者账户和兑换 | 当前核心 API 没有消费者身份依赖；小程序方案将账户、额度和兑换列为新增 | 必须独立落地，不由浏览器变量替代 |

依据：[R1][R2][R3][R4][R5][R6]。

### 1.2 需要优先处理的源码差异

| 优先级 | 证据位置与现状 | 风险或改法 |
| --- | --- | --- |
| P0 | `schemas/orders.py::Params.style_id` 允许 `None`；`services/prompts.py::build_initial_prompt` 会回退到保持原图风格 | 与小程序“双模式必选风格”不一致。新增消费者 schema 和服务端检查，不要为此破坏后台保留原图风格的既有能力 |
| P0 | `api/orders.py` 的订单、图片与风格 API 没有消费者 `current_user` 依赖；图片通过 ID 获取 | 当前依赖内部部署边界。开放消费者入口前补齐用户归属校验；UUID 和 `Cache-Control: private` 都不是授权 |
| P0 | `worker.py::_download` 本地写图后将任务置为成功并调用 `set_finals` | 消费者侧还需审核、标识、可交付验证及次数结算。引擎成功不能直接等于作品可见 |
| P0 | `services/generation.py` 的并发和幂等按订单/批次组织 | 新增用户维度限并发与幂等；用户创建多个订单不能绕开额度约束 |
| P1 | `services/prompts.py::validate_sources` 不允许 `material_slots` 和非空 `changes` 混用 | 小程序采用一种明确的映射协议，不能把逐图用途和旧长提示词混成两个冲突来源 |
| P1 | 原型 `works` 存的是风格索引，`references` 存的是字符串数组 | 两个同风格作品无法表达独立身份；参考图编号、替换及删除需要稳定 ID |
| P1 | 原型 `persist()` 在快照较大时删除 `references`，文字 input 仅更新内存 | 这是演示限制，不可迁入生产；不能承诺草稿保留却静默丢失参考图说明 |
| P1 | `services/orders.py::write_session` 使用 SQLite 的 `BEGIN IMMEDIATE`；Worker 使用本机文件锁 | 转 PostgreSQL 和多实例时不能只换连接字符串，需要改事务及任务领取机制 |

依据：[R2][R5][R6][R7][R8][R9]。以上是静态代码差异，不是声称已经在线复现的漏洞或故障。

## 2. 产品定位与名称

对外一句话建议：**上传自己的照片，选喜欢的风格，做一张新的头像。**

参考图模式的补充解释：**借用参考图里的服装、配饰或背景细节，做成你的头像。**

不要将“AI 多图编辑引擎”“主素材融合”“提示词构建”作为用户首页语言。AIFACE 可暂保留为品牌；用户首先需要知道它能做什么，不必先理解英文缩写。

两种模式建议改名：

| 内部模式 | 用户看到的名称 | 一句解释 |
| --- | --- | --- |
| `photo` | 照片变头像 | 一张自己的照片，生成喜欢的头像风格 |
| `reference` | 参考图定制 | 指定参考图里的细节，融入自己的头像 |

“参考图定制”不是把参考图整张重绘，也不是自动换脸。模式卡下用小图解表达：`你的照片 + 参考图中的帽子 + 日系动漫 → 新头像`。小图解只做结构说明；正式效果图必须使用有授权且经过实际生产链路验证的素材。

6～8 种风格在现有文档中属于建议名单，不是已验收能力。建议先集中验收 3～4 种差异足够明显的风格，再按结果扩充，不为凑数量上架。这个分批验收不影响双模式必须同时保留。

## 3. 信息架构：三个一级入口，一条制作流程

一级 Tab 保持：**做头像 / 我的作品 / 我的**。不要增加“充值”独立 Tab。

建议路由：

```text
做头像（一级）
  ├─ 风格详情（子页，可保留既选模式）
  └─ 制作头像（子页，共用一套页面，根据 mode 展开参考图区）
       ├─ 登录确认（弹层/子页，完成后原地续接）
       ├─ 兑换次数（子页，记录来源和 draft_id）
       └─ 生成进度 → 作品详情
我的作品（一级）
  └─ 作品详情 / 原任务进度
我的（一级）
  ├─ 兑换次数 / 兑换记录
  ├─ 帮助与反馈
  └─ 隐私与账号
```

一级页用底部 Tab；制作、进度、结果、兑换用子页导航，不在每个子页都重复展示 Tab。这样底部主操作条不需要再与 Tab 争位置。返回应遵循进入来源，而非全部跳回首页。原型购买页固定返回制作页，正式版必须改为 `return_to + draft_id`。[R2]

## 4. 各页面 UI 和交互整改

### 4.1 首页：先看到效果，再决定怎么做

现有原型在风格网格前叠放标题、副标题、模式切换、领取次数卡；参考模式又加一段说明和开始按钮，首屏会被说明性元素挤占。风格卡本身目前只是 CSS 人像占位，不可作为正式样张。[R2]

建议顺序：小型品牌栏 → 一句主标题 → 双模式选择 → 紧凑次数提示 → 两列风格图。新人额度用一行说明，而非抢占视觉中心的大营销横幅。

```text
AIFACE · 头像                         新人可领 3 次
做一张，更喜欢的自己

[ 照片变头像 ]       [ 参考图定制 ]
一张照片，变成喜欢的风格

柔彩蜡笔               日系动漫
[真实方形样张]          [真实方形样张]
柔和彩色笔触            清透、轻盈的动漫感

黏土公仔               复古漫画
[真实方形样张]          [真实方形样张]
立体软萌感              暖色与漫画笔触

做头像                 我的作品                 我的
```

模式差异通过一句解释展示，不加第二套导航。用户点风格卡进入详情，再用“用这个风格”继续制作；已经登录且熟悉流程的用户可提供明确的快速入口，但不是首版必需功能。

样张建议每种风格先用同一授权人物、相近构图作横向比较，详情页再增加不同长相样本。不能只比较四张不同人物的漂亮图片，因为那难以分清是风格还是人物差异。

### 4.2 风格详情：让用户理解买到什么

主内容为实际样张、原图/成图对照、圆形预览、风格一句话说明和适用照片提示；按钮为“用这个风格”。样张注明“效果示例，实际结果因照片而异”。

圆形仅作为头像裁切预览，首版仍交付方形原图，不为了预览另起一次生图或扣次。不承诺“100% 像本人”“完全复刻”。

详情页保留 `mode`，不能用户从参考图定制选风格后，被切回照片模式。

### 4.3 制作页：一套页面，分区展示，不拆成很长的向导

**照片变头像**：你的照片 → 所选风格 → 底部生成。

**参考图定制**：你的照片 → 参考图与用途 → 所选风格 → 底部生成。

“你的照片”上传前显示示例要求；上传后显示缩略图及“查看 / 更换 / 移除”。“单人、脸部清晰、光线充足”足够，不向用户抛 JPEG MIME、Asset ID 等内部术语。

风格不用原型里的纯文字 `select`，改为“风格缩略图＋名称＋更换”卡片；点开底部风格图册。未选风格时明确显示“请选择头像风格”，两种模式都不得默认为原图风格。[R2][R7][R8]

表单说明不重复堆放“统一风格、默认私有、失败返还”等整段文字。必要解释随当前控件就近出现；详细政策进入可点击说明。

### 4.4 参考图卡：最需要投入的交互模块

建议每张参考图的结构：

```text
参考图 1                               查看 / 更换 / 删除
[图片缩略图]
想参考哪里？
[服装] [配饰] [发型] [背景] [其他]

具体想怎么改？
例如：把这张图的帽子加到我头上，保留我的发型。
[用户可编辑的说明输入框]

已选择：配饰；应用位置：人物头部
```

用途标签只帮助填入一个可编辑起点，不是偷偷替用户决定元素。参考图有两顶帽子、多个角色、复杂背景时，仍需具体说明选择哪一部分。不要给每张图再强加一套必须填满的复杂专业表单。

标题下面始终区分“你的照片决定人物；参考图只提供你指定的细节”。填写位置和用途后，生成区可展示一句简短摘要，如“参考图 1：帽子；参考图 3：背景花朵”。这比让用户审查整段模型提示词更合适。

删除与编号规则：每张参考图有 `reference_id` 和递增的 `display_no`；删除参考图 2 后，参考图 3 仍叫参考图 3。替换图片时保留同一卡片身份，但提示用户重新核对原说明。不要将 UI 数组下标用作身份。

保持参考图数量可配置，从后端能力配置返回最大输入数、单图上限和允许格式；没有实测依据，不擅自写死“最多 1 张”，也不在界面承诺无限张。

### 4.5 底部操作条与错误提示

制作页底部固定一条主操作区域，上方预留足够滚动空间：

```text
可用 3 次                 本次成功生成使用 1 次
[                  生成头像 · 1 次                  ]
```

有免费余额时显示“免费生成 · 1 次”；总可用次数不足时显示“兑换次数后继续”，并保留全部草稿。提交中显示“正在提交…”，不得连续创建新请求。

字段校验失败应在对应位置显示原因，并滚动至首个错误；不能只在长页面末端显示一条消息。存在无效字段时，可以让按钮点击触发校验定位；若使用禁用按钮，则必须同时显示原因，不能让用户猜。

软件键盘弹出时，参考图输入框不能被固定操作条挡住。允许键盘收起后再展示完整额度说明，但主操作和页面滚动逻辑需实机验证。

### 4.6 生成中：退出不丢任务，不表演百分比

现有原型点击生成即减少内存次数并跳结果页，只是模拟流程；需要新增真实进度页与任务恢复。[R2]

建议展示后端确实知道的阶段：等待处理、正在生成、正在整理头像、已完成。后端没有详细子进度时，只显示“正在生成”，不伪造 85%、99%，也不写未经测量的“10 秒完成”。

提示语：“可以先离开，完成后到「我的作品」查看。”这是结果存储承诺，不等于已接入订阅通知，首版不要附带未实现的消息提醒承诺。

网络断开只表示当前无法查询，不意味着模型失败。“重新查看进度”查询原任务；不能把查询失败包装成“重新生成”。

### 4.7 结果与作品

结果页：大图 → 方形/圆形预览 → “保存到相册”主按钮 → “换个风格再做”次按钮 → 一行说明“再次生成使用 1 次，重复保存不扣次”。

“保存到相册”必须等待真实保存成功后才提示成功；权限被拒、下载地址过期、网络断开分别处理。不能声称自动更换微信账号头像。

“换个风格再做”恢复原制作草稿，保留原主照片、参考图和说明，用户重新确认风格后提交新任务。该动作从原始制作素材重新创作，不暗中把当前成图当主照片。未来提供“修改这张成图”时，应明确成图作为底图且产生新作品，不能与换风格混用。

我的作品按 `generation_id` 组织而非风格索引；同一风格连续生成两次仍是两个作品。列表展示进行中、已完成、待处理；生成失败卡保留原因和“调整后再生成”，不能静默消失。

作品详情中可提供不强制的效果反馈“很喜欢 / 需要改进”，后者细分“不太像本人、参考细节不对、风格不符”。这是质量定位建议，不自动等价于系统失败或无条件返还次数；不满意补偿政策必须单独定稿。

### 4.8 激活兑换码：兑换生成次数

当前个人认证主体无支付功能。将原购买入口改为“兑换次数”，展示激活兑换码输入框、兑换按钮、结果及“兑换记录”，不展示商品、价格或支付按钮。

服务端分别返回免费可用、兑换可用和处理中占用次数。兑换成功显示“已增加 N 次”，兑换处理中不提前加余额；失败保留当前草稿，不在本地草稿或埋点保存完整兑换码。每码次数、码的兑换截止时间与到账权益有效期分别定义，不能混用。

从“我的”进入兑换后返回“我的”；从制作进入兑换后依据 return_to + draft_id 回到同一草稿。成功后仍由用户点击生成，不自动调用模型。同账号重复兑换已成功的码返回原结果，不再次到账；不可用码提示“兑换码不可用，请核对后重试”，不显示其他兑换者的信息。

## 5. 建议界面文案字典

以下是拟上线文案，必须在相应能力完成后展示；隐私、保存期限、费用与补偿类文案不能先承诺再补实现。

| 位置 | 现有或易误用表达 | 建议表达 |
| --- | --- | --- |
| 品牌说明 | AIFACE · 头像 | 可保留；搭配明确功能标题 |
| 首页标题 | 换个头像，换种心情 | 做一张，更喜欢的自己 |
| 模式一 | 主照片生成 | 照片变头像 |
| 模式二 | 参考图编辑 | 参考图定制 |
| 主照片标题 | 主照片 · 作为基础画面 | 你的照片 |
| 上传按钮 | 上传主照片 | 上传我的照片 |
| 主照片要求 | 复杂格式说明 | 单人、脸部清晰、光线充足 |
| 参考图区 | 参考图 · 只提取你指定的元素 | 添加想参考的图片 |
| 参考图区说明 | 素材融合 | 只参考你指定的细节，不替换你的人物身份 |
| 逐图输入标签 | 提取什么，改到哪里？ | 这张图里，你想参考哪里？ |
| 输入示例 | 请输入提示词 | 例如：把这张图的帽子加到我头上，保留我的发型。 |
| 添加下一张 | 继续添加参考图 | 再加一张参考图 |
| 风格字段 | 成图风格 · 必选 | 头像风格 |
| 风格空值 | 下拉选择 | 请选择头像风格 |
| 新用户提示 | 去领取 | 领取 3 次免费体验 |
| 首次登录 | 注册账号 | 登录并领取 3 次 |
| 已有账户登录 | 登录并领取 3 次 | 登录并继续（不要承诺重复赠送） |
| 免费生成 | 生成编辑后的头像 · 使用 1 次 | 免费生成 · 1 次 |
| 普通生成 | 开始任务 | 生成头像 · 1 次 |
| 未上传 | 请求参数不正确 | 先上传一张你的照片 |
| 缺少参考图 | 缺少素材 | 添加至少一张参考图，或切换到「照片变头像」 |
| 缺少逐图说明 | 说明不完整 | 补充参考图 2 的用途，再继续生成 |
| 失效引用 | invalid_source | 参考图 2 已移除，请重新添加或修改说明 |
| 开始提交 | 请等待 | 正在提交… |
| 后台处理中 | running / queued | 正在生成头像 / 等待处理 |
| 查询暂不可用 | 生成失败 | 暂时无法更新进度，稍后可继续查看 |
| 确认失败且已解冻 | 失败自动退款 | 本次未生成成功，未使用次数 |
| 状态不明 | 补生成、接受重复计费 | 正在确认生成结果，请勿重复提交 |
| 输入不通过 | 审核失败 | 这张图片暂时无法用于生成，请更换图片；未使用次数 |
| 相册权限拒绝 | 保存失败 | 未获得相册保存权限，可前往设置后再保存 |
| 余额不足 | 请充值 | 次数不足，兑换后继续；已保留当前制作内容 |
| 空作品页 | 暂无数据 | 还没有作品，先做一张喜欢的头像吧 |
| 隐私提示 | 照片绝不上传第三方 | 照片用于头像制作，作品默认仅自己可见。处理方式详见隐私说明。 |

说明：最后一项仍需真实披露模型服务商处理、保存时长等信息。“默认仅自己可见”不等于“管理员永远无法访问”或“图片不传给模型服务商”。

## 6. 视觉规范与移动端细节

保留现有松绿色方向：背景 `#F7F8F5`、正文 `#202724`、品牌主色 `#24694F`；白色卡片、16px 级圆角。头像图片提供主要色彩，避免背景、按钮和装饰都抢注意力。[R1][R2]

以 375px 手机逻辑宽度设计，验收覆盖 320、375、390、430 等宽度；左右边距建议 16px，卡片间距 12px；两列风格图为主。正文 14～16px、辅助说明一般不小于 12px。重要操作的实际可点击区域建议至少 44px，不把很小的文字当作唯一点击区域。

这些数值是设计建议，不是宣称所有微信终端的统一硬性规范。转为 Taro/rpx 后仍需检查真实逻辑像素尺寸，不能仅凭设计稿换算认为点击区域达标。

选择状态使用边框、对勾与文字共同表达，不只靠颜色。主照片和作品预览优先完整显示；列表卡可居中裁切，详情则让用户看清实际输出边界。圆形预览中额头、发饰、下巴不能被意外截断。

首版尽量用标准导航栏，不在正式客户端手画原型里的微信胶囊。制作页底部适配安全区，并用等高占位避免遮住最后一个输入框。原型中的 `light-dark()`、浏览器 DOM 操作和整页 `innerHTML` 不直接迁移为小程序实现；主题值通过小程序可验证的样式与状态显式控制。[R2][E1]

## 7. 技术结构：保留引擎，新建消费者边界

### 7.1 推荐目录

```text
AIFACE/
├─ frontend/                       # 保留：经营后台
├─ miniprogram/                    # 新增：Taro + React + TypeScript
│  └─ src/
│     ├─ pages/home/
│     ├─ pages/style-detail/
│     ├─ pages/create/
│     ├─ pages/generating/
│     ├─ pages/result/
│     ├─ pages/works/
│     ├─ pages/account/
│     ├─ pages/redeem/
│     ├─ pages/redemption-history/
│     ├─ pages/privacy-account/
│     ├─ components/StyleCard/
│     ├─ components/PhotoSlot/
│     ├─ components/ReferenceCard/
│     ├─ components/GenerationBar/
│     ├─ components/CreditSummary/
│     ├─ components/TaskStateCard/
│     ├─ stores/creation-draft.ts
│     ├─ services/api.ts
│     ├─ services/auth.ts
│     ├─ services/media.ts
│     ├─ services/redemption.ts
│     ├─ copy/zh-CN.ts
│     └─ types/contracts.ts
├─ backend/app/api/consumer/        # 新增：只面向已认证用户的业务接口
├─ backend/app/schemas/consumer.py
├─ backend/app/services/auth.py
├─ backend/app/services/avatar_creations.py
├─ backend/app/services/consumer_prompts.py
├─ backend/app/services/credits.py
├─ backend/app/services/redemptions.py
├─ backend/app/services/moderation.py
├─ backend/app/services/storage.py
├─ backend/app/services/user_delivery.py
└─ backend/tests/consumer/          # 新增：权限、账本、状态和双模式回归
```

Taro 适合延续 React/TypeScript 经验，但它仍遵循小程序的组件、API、路由规范；不能把 Next.js 页面和浏览器能力直接复制进去。独立锁定小程序端验证通过的依赖，不照搬后台 React 版本，也不把“最新版本”当成兼容性保证。[E1]

### 7.2 原有代码如何连接，而不是假装已经有新接口

建议新增 `AvatarDraft` 与 `AvatarGeneration` 消费者实体。一个草稿映射一个内部 `Order` 创作容器，上传素材仍归属于该容器；一次生成对应一个独立 `AvatarGeneration`，关联现有 `GenerationBatch`。

小程序看见的是草稿和作品，不是商家的客户名、交付订单和供应商 task ID。内部 Order 继续承担旧引擎素材归属和输入快照职责；兑换记录独立保存，不复用商家订单状态。

创建草稿时由服务端建立内部容器，填充内部所需字段；不能让消费者手工输入虚假的客户姓名。旧商家数据 `owner_user_id` 可为空，仅后台访问；不能自动归属给第一个登录用户。

上传通过当前草稿授权完成；每个 asset 必须满足“属于当前用户、属于当前容器、已上传完成且可用于输入”。更换图片创建新的素材版本，不覆盖运行中任务引用的旧文件。生成提交时冻结素材、用途、风格、模型、输出规格和内容策略快照，后续编辑不能改变已排队任务。

原有 `create_initial()`、`make_task()`、供应商提交/查询恢复尽量复用。新增消费者协调服务统一做账户校验、模式编译、额度冻结、生成记录和批次关联。原有后台完成/关闭订单规则保留；消费者作品不依赖人工点击“完成订单”。[R5][R6][R9]

### 7.3 草稿数据结构

```ts
type CreationMode = 'photo' | 'reference';

type ReferenceDraft = {
  referenceId: string;       // 稳定身份，不是数组下标
  displayNo: number;         // 1、3 允许不连续；删除不重新编号
  assetId: string | null;
  suggestedType: 'clothing' | 'accessory' | 'hair' | 'background' | 'other' | null;
  instruction: string;       // 用户最终确认的逐图说明
  uploadState: 'empty' | 'uploading' | 'ready' | 'failed';
};

type CreationDraft = {
  draftId: string;
  revision: number;
  mode: CreationMode;
  mainAssetId: string | null;
  styleId: string | null;
  references: ReferenceDraft[];
  nextReferenceNo: number;
  activeGenerationId: string | null;
};
```

切换至 `photo` 时保留 references 作为草稿，但构建提交请求时必须显式输出空引用集合；服务器收到 photo 模式非空 references 应拒绝，而不是猜测其是否应该生效。切回 reference 时恢复原卡片和说明；切换风格不清空编辑说明。

本地草稿按账号隔离，文本输入防抖保存，页面隐藏时补存，登录/兑换返回时恢复。仅存必要 ID 和文字，不将大图 Base64、完整签名地址、余额作为持久真相。临时图片路径恢复后需检查可读性，已失效则提示重选。退出账号清除对应本地草稿与缓存，避免换号看到上一位用户内容。

### 7.4 建议接口契约

以下全部为新增设计，统一前缀 `/api/v1`；旧 `/api/orders` 不作为消费者入口。

| 方法与路径 | 职责与安全边界 |
| --- | --- |
| `POST /auth/wechat` | 服务端验证微信登录凭证，关联平台身份，签发本系统会话；幂等发放新人权益 |
| `GET /me` | 返回资料与实际可用/占用次数，不让客户端推算权威余额 |
| `GET /capabilities` | 返回当前模式、可用风格、输入数量/格式限制、兑换入口可用性 |
| `GET /styles` | 仅返回上架风格及公开展示字段，不泄露完整提示词和管理配置 |
| `POST /drafts` | 新建个人草稿及内部创作容器 |
| `PATCH /drafts/{id}` | 版本校验更新草稿；归属校验 |
| `POST /drafts/{id}/assets` | 登录后上传；校验 MIME/内容/尺寸及归属，返回 asset_id |
| `POST /generations` | 前置校验、额度冻结、创建唯一生成任务；必须有幂等键 |
| `GET /generations/{id}` | 查询当前用户的原任务，包括公开状态和额度处理状态 |
| `GET /generations/by-request-key/{key}` | 解决提交已受理但客户端没收到任务 ID 的恢复问题 |
| `GET /works` | 只列本人的作品/在途任务，分页且按 generation_id 区分 |
| `POST /works/{id}/download` | 只对可交付作品签发短期下载地址；不扣次 |
| `DELETE /works/{id}` | 逻辑隐藏与异步清理，按留存政策处理，不能删除次数账本 |
| POST /redemptions | 登录用户提交兑换码和幂等键；码认领与次数入账同事务，客户端不指定次数 |
| GET /redemptions | 分页返回本人兑换记录及脱敏码标识，不返回完整码 |
| GET /redemptions/by-request-key/{key} | 归属校验后恢复本次兑换结果；未查询到时可沿用原键重试，不推断已到账 |
| `POST /account-deletion-requests` | 账号注销请求；作品、照片、账务留存按正式政策处理 |

示例生成请求：

```http
POST /api/v1/generations
Authorization: Bearer <session-token>
Idempotency-Key: <此次用户操作的稳定UUID>
```

```json
{
  "draft_id": "draft_123",
  "draft_revision": 7,
  "mode": "reference",
  "main_asset_id": "asset_main",
  "style_id": "style_anime",
  "references": [
    {
      "reference_id": "ref_hat",
      "display_no": 1,
      "asset_id": "asset_hat",
      "instruction": "使用这张图左侧人物的帽子，戴到主照片人物头上；保留我的发型。"
    },
    {
      "reference_id": "ref_flower",
      "display_no": 3,
      "asset_id": "asset_flower",
      "instruction": "使用这张图右下角的花朵作为背景装饰，不替换人物。"
    }
  ]
}
```

客户端不传价格、余额、模型密钥、供应商地址或可任意覆盖的系统提示词。`style_anime` 是示例公共 ID，服务端映射到已经上架的内部风格记录；不能直接传给目前仅接受 `q_crayon_001/custom_...` 的旧 Params。[R7]

建议返回：

```json
{
  "generation_id": "gen_123",
  "status": "queued",
  "credit_status": "reserved",
  "credits_available": 2,
  "credits_reserved": 1,
  "request_id": "trace_123"
}
```

### 7.5 服务端双模式验证与提示词编译

校验顺序：身份 → 草稿归属与版本 → 模式 → 已上架风格 → 主图有效性 → 对应模式引用校验 → 输入审核结果 → 用户在途任务与余额 → 冻结及建任务。重要条件在实际建任务事务中复验，不能只信前端预检。

`photo`：必须主图和有效风格，不允许 reference 请求字段携带有效素材；风格缺失明确报错。

`reference`：必须主图、有效风格、至少一张参考图；每张有用途说明，引用不重复且归属正确；数量上限来自能力配置。普通文字无法完全判定语义是否明确，对常见错误用就近提示，不宣称系统能自动理解所有模糊指令。

建议新增独立消费者编译器，输出统一 `GenerationSpec`（输入顺序、提示词、风格/模式/输出规格快照），再将现有创建批次时的快照落库与建任务逻辑提取为共用内部函数。旧后台仍用 `build_initial_prompt()`，消费者走 `compile_consumer_spec()`；两者复用后续 Worker。这样不必为了迁就旧参数让用户填写长提示词。**不要同时启用旧 `changes` 和 `material_slots` 两套说明来源。** 现有代码会拒绝这种混用。[R8]

若工程阶段先使用旧 `changes` 作为兼容层，必须令 `material_slots=None`，同时明确提供 `target_description/change_type/source_asset_ids/instruction`。不能把所有参考图的目标和用途都填成相同占位值，否则现有冲突检测会将不同说明判为冲突；也不能通过乱造目标名绕过冲突检查。兼容层须有专项测试，最终以用户确认的逐图关系为准。

稳定映射例子：

```text
模型图片 1 = 你的照片
界面参考图 1 / ref_hat    -> 模型图片 2
界面参考图 3 / ref_flower -> 模型图片 3
```

UI 编号与模型顺序明确分别存储，由服务端编译；不依赖用户自由文本里的“图 2”作为唯一关联依据。

建议提示词分层：人物身份与基础内容约束 → 逐图用途 → 全图统一风格 → 1:1 头像取景 → 安全/输出要求。当前引擎模板默认保留底图布局和背景，消费者模板要明确头像居中、合理留白的取景优先级，避免同时要求“原布局完全不变”和“全新头像构图”。如用户指定保留某内容又要求替换同一内容，应先提示冲突或按清晰规则由用户确认，不静默决定。

供应商调用仍由服务端完成。用户说明是图像创作数据，不允许变成改变价格、模型地址、账户身份或系统权限的指令。

## 8. 任务状态、次数账本与异常恢复

### 8.1 分开引擎状态与消费者状态

现有引擎已经区分 `submission_unknown`、下载失败和明确失败，并尽量恢复原任务。保留这个价值，不为消费者界面“简单”而删掉内部状态。[R5][R6]

消费者公开状态建议如下，名称可调整但语义不能丢失：

| 公开状态 | 用户显示 | 次数状态与动作 |
| --- | --- | --- |
| `queued` | 等待处理 | 已冻结 1 次；可离开 |
| `generating` | 正在生成头像 | 仍冻结；不再次提交 |
| `finalizing` | 正在整理头像 | 下载、审核、标识、存储仍可能进行，不提前扣次或放图 |
| `reconciling` | 正在确认生成结果 | 不自动重发；后台核对原任务 |
| `ready` | 头像已做好 | 可领取后核销一次；重复保存不扣 |
| `failed` | 本次未生成成功 | 冻结释放成功后才显示“未使用次数” |
| `rejected` | 暂时无法提供这张头像 | 不交付不合规结果，按规则释放冻结 |

`credit_status` 单独使用 `reserved / captured / released / compensated` 等账务状态，不让所有含“失败”的分支都执行一次加次。

### 8.2 额度表与不变量

建议实体：users、platform_identities、avatar_drafts、avatar_generations、credit_accounts、credit_ledger、credit_reservations、redemption_batches、redemption_codes、redemptions。素材和内部创作容器加用户归属；历史商家记录仍仅后台可见。

关键唯一约束：

```text
platform_identities(appid, openid)
avatar_generations(user_id, idempotency_key)
avatar_generations(engine_batch_id)
credit_ledger(business_key)
credit_reservations(generation_id)
redemption_codes(code_digest)
redemptions(code_id)
redemptions(user_id, idempotency_key)
```

credit_ledger 每条变动包含用户、来源批次、free/redeemed 来源、可用与冻结变化、业务编号、关联生成或兑换记录；只追加更正记录，不直接改历史。兑换入账 business_key 绑定唯一兑换记录；人工纠错按来源权益处理并留审计，不随便扣掉其他兑换批次或赠送次数。

账户操作示意：

```text
赠送：available +3
冻结：available -1, reserved +1
成功：reserved -1, consumed +1
失败：reserved -1, available +1
```

所有计数为整数。免费优先、兑换其次；从哪个来源冻结，失败就解冻回哪个来源。用户可用次数与冻结次数不可为负；每次生成最多冻结一次、最多结算一次。

### 8.3 原子提交和 Worker 完成边界

```text
短事务：
  校验并锁定当前用户额度账户
  查询同用户幂等键：存在则校验请求摘要并返回原任务
  复验当前无运行中任务、输入归属和余额
  新建 AvatarGeneration + 关联引擎批次/待执行任务
  冻结 1 次，追加账本
  提交

事务外：
  Worker 调模型 / 查询 / 下载原任务结果

完成处理：
  输入与输出审核通过
  输出规格合格，完成需要的 AI 标识
  私有存储对象写入成功且可领取
  短事务锁定生成记录与冻结记录
  原子发布用户作品并核销次数
```

不在数据库事务里等待远程生图或长时间内容审核。外部对象存储无法与数据库一起事务提交，应先写不可变隔离对象，再在数据库中发布可见指针；进程崩溃后的孤立对象由清理任务回收，已发布对象不得被错误清理。

当前 `_download()` 会直接调用 `set_finals`，应增加消费者渠道分支或统一交付服务：后台原流程继续工作，消费者必须完成其额外门槛。仅在小程序页面隐藏审核中图片不够，下载 API 同样拒绝未发布资产。[R6]

### 8.4 幂等和晚到结果

同一用户点击一次生成产生一个幂等键。响应丢失时，沿用原键查单或重发同一提交；相同键不同内容返回冲突，不自动创建第二个任务。用户明确发起新的创作才生成新键。

明确失败可以释放冻结；请求超时或受理不明不能认定失败。继续后台核对原任务，不要求消费者勾选“接受重复计费”；这种供应商不确定性应由平台处置，后台保留审计能力。

需要预先定稿超时补偿期限。超过期限补回用户次数时记录唯一补偿业务编号；原任务之后成功也不能偷偷再扣一次。建议首版将这种晚到可交付结果免费提供，平台承担成本；该条是建议运营规则，公开前须确认预算和实现。

下载恢复不创建新的生图任务，也不重新扣次。用户手机保存失败不等于服务端交付失败：服务端已提供合格可领取作品时次数已消耗，恢复相册保存应免费。

### 8.5 数据库与任务领取

当前 SQLite 的 `BEGIN IMMEDIATE` 是既有并发语义的一部分；迁 PostgreSQL 要改事务入口、账户行锁、幂等冲突处理，并重新跑并发回归。不能做“换个 DATABASE_URL 就完成”的计划。[R9]

正式消费者服务建议独立 PostgreSQL 与私有对象存储；本地后台保留原数据库，迁移做备份和独立验证，不覆盖旧业务库。首版可只部署一个 Worker，前提是实测容量满足灰度用户，且部署机制保证不同时启动两份。

扩到多 Worker 前，改成数据库行锁领取与租约：记录 `lease_owner / lease_until / claimed_at` 等，领取采用适合 PostgreSQL 的并发队列模式，远程提交受理不明仍需核对。原有本机文件锁不能成为跨机器的互斥保证。[R6]

## 9. 登录、照片处理、兑换和上线门槛

### 9.1 登录与会话

建议游客可看风格；主动制作时登录。服务端验证微信登录凭证，使用同一 AppID 下的平台身份关联内部用户，再签发短期业务会话。前端不能通过传入任意 openid 冒充用户。昵称、头像设置是独立体验，不假定登录自动取得用户资料。

新人 3 次在账户事务中幂等发放；重登、清缓存不重送。同一账号一次不代表同一自然人一次，风控结合速率和异常行为，不因同 IP 直接认定同人。已有账户登录使用“登录并继续”，返回后恢复操作而不自动生成。

微信登录和部分媒体官方页面本次直接读取失败，接口调用、隐私声明与基础库条件列为实施前核验项；不能据此声称已经跑通微信授权。

### 9.2 图片、资产权限与真实隐私

每次素材上传、任务查询、作品详情、下载签发和删除都校验 owner，必要时连带校验容器归属。管理 API、模型配置、风格写入、人工核对接口采用独立管理员权限和网络入口；不能通过隐藏菜单代替鉴权。

当前后台通过 Cloudflare Access 邮箱验证保护内部入口，不能把该浏览器身份方案直接用作小程序用户会话，也不能把本地电脑关机后仍会中断的部署描述成生产服务。[R3][R4]

建议上传链：用户选择图片 → 本地预览与必要预处理 → 隐私授权/用途告知 → 私有上传 → 服务端解码、尺寸与内容校验 → 校正方向、去除不必要定位元数据 → 输入审核 → 返回可用素材 ID。当前项目支持 JPEG/PNG/WebP 静态图的限制应在接入时校验；iOS 相机/相册可能返回的具体格式需真机覆盖，不预先宣称所有 HEIC 等格式均支持。[R3]

客户端预览与相册保存走后端批准的短期资源地址或鉴权下载，配置对应合法域名；地址过期只重新授权下载。签名地址不写日志、不永久存草稿。原片、参考图、生成图分开定义保存期限，并有删除与异常清理任务。

消费者页面不得写“照片不上传第三方”“绝不用于训练”，除非实际供应商协议和全链路处理确实支持这些承诺。应披露第三方处理者、用途、保存期限与删除方法，后台查看原片也应按权限和必要性审计。

### 9.3 激活兑换码与权益到账

用户已确认：当前个人认证小程序无支付功能，首版只实现激活兑换码增加次数。取消支付适配器、商品、支付订单、回调、退款和支付对账任务。兑换码是次数凭证，不替代微信登录；发放渠道尚待确定，不自行加入站外收款或购买引导。

建议首版每码只兑换一次，兑换固定正整数次数。管理员按批次生成高熵随机码，存储带服务端密钥的摘要及脱敏标识；规范化规则在生成与兑换时一致。完整码仅生成时向授权管理员交付，不进入普通日志、错误追踪、埋点或持久草稿；密钥版本须可追踪。批次记录每码次数、码有效期、创建者、发放额度与状态；生成、导出和停用均有管理员权限与审计。后台操作与消费者接口分离。

兑换事务：验证登录 → 按当前账号幂等键查原记录并比较码摘要 → 锁定码记录及对应批次、用户账户 → 同账号已成功兑换则返回原结果 → 对新兑换复验批次/码未停用、未过期且未使用 → 写入唯一兑换记录 → 认领码并增加对应来源权益、追加账本 → 一起提交。统一锁顺序，停用与兑换采用相同互斥边界。任一步失败全部回滚，不出现码已用但次数未到账；以数据库唯一约束保证跨账号并发最多一次成功。

同账号已成功兑换的码即使后来过期或批次停用，也应恢复原成功结果；其他账号不获知兑换者信息。响应丢失使用原幂等键查询或重试，同键不同码返回冲突。同账号以新键重复兑换同一码也返回原成功记录，不再次入账。若码摘要密钥轮换，应保留未过期码的验证能力。

设置账号及来源速率限制、连续错误限流和异常告警，控制猜码与批量尝试；不因同 IP 直接判定同人。停用未使用码只影响后续兑换，停用已兑换码或批次不得自动扣回用户次数。权益纠错需独立审计，不删除历史账本。

免费 3 次及兑换权益均按两种模式与参考图数量实测交付成本，涵盖失败调用、审核、存储和流量。费用未知不按零计；后台设置发码和赠送总额度预算。每码次数、兑换截止时间、到账次数有效期及发放渠道仍为待定事项；建议到账次数首版不设有效期。

### 9.4 内容审核、AI 标识和准入

输入照片、参考图与编辑说明都需纳入审核；输出审核通过后才能公开交付。审核服务出错属于审核未完成，不当作审核通过。

AI 标识不是结果页写一句“AI 生成”就算完成。适用的《人工智能生成合成内容标识办法》涉及显式提示、文件元数据隐式标识以及下载导出等环节；应结合业务适用情形实现，并核验重新编码、压缩和保存后标识仍满足要求。[E3]

首版不要以“免费无水印”作为承诺。存在特殊输出诉求时，单独核实适用规则，而不是直接移除标识。主体、类目、备案、模型服务相关要求、隐私政策、兑换与补偿规则及未成年人使用规则应在公开运营前按实际业务确认；本报告不等于已满足法律或平台审核条件。

## 10. 文件级实施任务

| 工作包 | 涉及位置 | 完成定义 |
| --- | --- | --- |
| 产品稿修订 | `docs/miniprogram-plan.md` | 固定双模式语义、命名、计费单位、超时补偿、草稿和引用行为；待定项明确列出 |
| 原型修订 | `outputs/avatar-miniprogram-ui.html` | 增加参考图稳定编号/删除/替换、视觉选风格、固定 CTA、真实状态模拟、正确返回；将购买页改为兑换页并模拟兑换记录；保留“演示”标签 |
| 小程序骨架 | 新增 `miniprogram/` | 真正可在微信开发者工具编译；使用独立依赖和 API 环境配置，无模型密钥或完整兑换码库 |
| 用户接口 | 新增 `api/consumer/`、`schemas/consumer.py` | 身份依赖、归属校验、风格必选、公开响应字段收敛；旧管理 API 不开放 |
| 双模式适配 | 新增 `consumer_prompts.py`、`avatar_creations.py`；适配 `prompts.py` | 稳定引用映射，photo 不混入隐藏素材，reference 必填，与旧参数互斥规则兼容 |
| 账户与账本 | 新增模型/迁移及 `credits.py` | 新人权益、冻结、扣次、解冻、补偿幂等，用户维度限并发 |
| 用户交付 | `worker.py` + 新增 `moderation.py`、`user_delivery.py` | 引擎成功不直接放行；审核、标识、存储、可领取和结算衔接 |
| 私有存储 | 新增 `storage.py` 并改图片访问边界 | 私有原片/输出、短期下载、过期恢复、删除清理，无跨用户读取 |
| 兑换闭环 | 新增 redemptions.py、兑换码模型、后台发码与停用接口 | 幂等认领、一次到账、响应恢复、限流和审计；码状态与账本一致 |
| 数据库生产化 | `db.py`、`services/orders.py::write_session`、Worker 领取方式 | PostgreSQL 事务/并发语义验证；原 SQLite 数据不被覆盖 |
| 测试与交付 | 新增 `backend/tests/consumer/`、小程序测试、更新验收文档 | 每条业务约束都有可重复的测试或真机记录，不用“能编译”代替验收 |

## 11. 分阶段实施，不承诺未经验证的排期

### 阶段 A：双模式可用原型

先将界面名称、风格展示、参考图卡和底部生成条定型；补齐登录返回、模式切换、错误状态和进度页模拟。用固定样张和模拟数据验证“用户是否知道上传哪张图、说明写哪里、是否会多扣次”。兑换使用演示码，不用原型假余额证明账本完成。

### 阶段 B：私有内测闭环

完成登录、个人草稿、双模式输入、服务端校验、免费 3 次账本、单任务生成、审核、可领取作品与保存。真模型效果测试另设明确授权和成本预算；本报告并未调用真实生图。

这一阶段就必须有 owner 隔离、服务端风格必选和准确结算。不要为了“先跑通”暂时把所有用户放进共享订单列表。

### 阶段 C：兑换与恢复

完成后台批次发码、停用、审计及前台兑换、一次到账、兑换记录、返回原草稿和异常恢复。使用隔离测试码验证同码跨账号并发、重复请求、事务中断、过期与停用、限流；不生成或发放正式兑换码。

### 阶段 D：上线验收

完成样张、效果、峰值容量、弱网、隐私和标识、数据备份恢复、终端兑换流程、合法域名/主体配置等验证。小流量灰度，再依据数据增加风格与并发，暂不加社区、会员或邀请系统。

## 12. 必须通过的验收用例

| 类别 | 用例 | 预期 |
| --- | --- | --- |
| 双模式 | 照片模式有遗留参考图草稿 | 请求不携带隐藏参考素材，生成结果不被其影响 |
| 双模式 | 参考模式没有参考图 / 空说明 | 服务端拒绝，不冻结、不发模型请求 |
| 双模式 | 任一模式缺少 style_id | 服务端拒绝，无原图风格回退 |
| 引用 | 删除参考图 2，参考图 3 仍有说明 | 3 不重新编号，模型映射准确 |
| 引用 | 同一卡更换图片 | 提示重新核对用途，旧已提交任务不变 |
| 草稿 | 改说明后直接退出、登录过期、兑换后返回 | 恢复同用户草稿；临时文件失效有明确提示 |
| 身份 | 换账号后恢复本地页面 | 不出现上一账号的照片或文字 |
| 权限 | A 用户读取 B 的 draft/asset/job/work/download | 全部拒绝；不知道对象 ID 不是唯一保护 |
| 赠送 | 首次登录双并发；清缓存后重登 | 仅发放一次 3 次权益 |
| 并发 | 仅剩 1 次，同时从两个草稿生成 | 最多创建一项可执行任务，不超扣 |
| 幂等 | 同一操作点击/重放多次 | 返回同一任务，只冻结一次 |
| 幂等 | 同一键改成不同请求内容 | 返回冲突，不生成第二张 |
| 提交恢复 | 后端已接单但响应丢失 | 使用幂等键恢复原 task ID，不重新生图 |
| Worker | 供应商提交中进程退出，受理不明 | 进入核对，不盲目重发 |
| Worker | 供应商成功，原图下载失败 | 恢复原结果，不新增生图扣次 |
| 审核 | 模型出图但审核未结束/服务故障 | 不向用户放图、不提前核销 |
| 原子性 | 文件写入后、事务提交前崩溃 | 重启恢复，无重复作品或重复扣次 |
| 补偿 | 超时补偿后旧任务晚到成功 | 不重新扣次，按定稿策略处理结果 |
| 保存 | 相册拒绝权限、短期地址过期 | 引导授权或刷新下载；不扣新次数 |
| 作品 | 同风格生成两次 | 两条不同作品，点击后各自正确 |
| 兑换 | 客户端提交完成但服务端未确认 | 显示处理中，不预加次数，按原幂等键恢复 |
| 兑换 | 同码被两个账号并发兑换，同账号同键或新键重试 | 全局一次到账；同账号返回原结果，其他账号不获知兑换者信息 |
| 兑换 | 码过期/停用、同键不同码、事务中断、并发停用 | 新兑换拒绝或完整回滚；已成功兑换可恢复，余额不被自动回收 |
| 视觉 | 小屏、大字体、键盘、底部安全区 | 主按钮与最后一个输入不被遮挡 |
| 效果 | 不同脸型/肤色/眼镜/发型；两种模式 | 分别记录相似度、参考细节、风格、构图，不能只验漂亮样张 |
| 效果 | 多张参考图指向同一部位或给出冲突说明 | 明确提示/确认，不静默错配 |
| 发布 | 下载文件经过再编码 | 必要 AI 标识仍满足要求，不能只检查页面标签 |
| 运维 | 备份恢复、Worker 停止、审核服务不可用 | 可恢复、有告警、不重复扣次 |

UI 能点通、接口返回 200、模型出了图，分别只是部分验收；三者都不等于消费者完成一次合格、扣次正确的交付。

## 13. 数据指标与优先顺序

首版记录一条真实路径：风格查看 → 开始制作 → 登录成功 → 主图上传完成 → 参考说明完成（参考模式）→ 提交 → 合格交付 → 相册保存 → 兑换成功。事件只记录必要 ID/状态和耗时，不把自拍、说明全文、openid 或签名地址发入行为分析。

两种模式分开观察首次成功交付率、上传失败率、参考说明放弃率、模型及审核失败率、等待时长、单张有效交付成本。再看免费三次用完后的兑换完成率。先定位流程是否能用、结果是否可靠，再谈更多风格和营销功能。

**最优先的五项：改清楚双模式名称；做可编辑的参考图用途卡；固定唯一生成主按钮；服务端落实必选风格与个人资产隔离；把可交付作品与次数账本做成一致的结算流程。**

## 14. 证据与外部核验

原源码与原型审查证据固定于首页 commit；本次兑换码需求来自用户同日确认，docs/miniprogram-plan.md 已同步修订，不能将新增设计视为该基线已实现功能。

| 编号 | 读取文件或官方来源 | 主要用途 |
| --- | --- | --- |
| R1 | `docs/miniprogram-plan.md` | 已确认小程序、双模式、必选风格、新人 3 次及候选范围 |
| R2 | `outputs/avatar-miniprogram-ui.html` | 页面结构、文案、模拟 state、引用与草稿行为 |
| R3 | `README.md` | 当前统一创作流程、部署边界和项目运行说明 |
| R4 | `backend/app/main.py`、`backend/app/api/orders.py` | 当前路由、订单/图片读取和管理边界 |
| R5 | `backend/app/services/generation.py` | 幂等、订单内并发、快照、重试及核对 |
| R6 | `backend/app/worker.py` | 文件锁、领取、远程恢复、写图后成功与交付 |
| R7 | `backend/app/schemas/orders.py` | 当前 Params、可空风格、素材/比例格式约束 |
| R8 | `backend/app/services/prompts.py` | 风格回退、参考映射、changes 与 material_slots 互斥 |
| R9 | `backend/app/services/orders.py` | SQLite 写锁、素材归属、旧订单语义 |
| E1 | Taro 4.x 文档“概述”，`https://docs.taro.zone/docs/react-overall` | React 与小程序组件/API/路由的差异 |
| E3 | 国家网信办《人工智能生成合成内容标识办法》，`https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm` | 显式/隐式标识与下载导出等适用要求 |

登录与部分媒体 API 官方入口原审查读取未成功，实施前仍需核验。原支付资料不再作为首版实施依据；本次依据用户提供的个人认证与无支付功能约束修订文档，未重新核验平台政策，也不据此保证兑换入口审核通过。


## 15. 本次需求修订记录

2026-09-24：用户明确“个人认证小程序，没有支付功能，只能用激活兑换码”。本次将消费者在线购买改为兑换次数，同步调整路由、接口、数据实体、账本来源、工作包和验收；保留新人 3 次、双模式、必选风格和生成交付边界。兑换次数码的一次性规则、安全和事务为实施建议，发放渠道、每码次数和期限待定。现有 HTML 原型未改，购买页面仍需按本方案替换；本次未实现业务代码、运行迁移或发放正式码。
